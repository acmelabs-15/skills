# Rulings log — walkthrough of the twelve prompts

Running record of owner rulings made during the prompt-by-prompt review, and where each one lands. Patch cadence: each prompt is patched immediately after its review; rulings that ripple to later prompts are logged here as PENDING and applied when that prompt is reviewed. A ruling marked APPLIED names the file already patched.

Started 2026-07-27. The ledger's D-5 carries the dated end-state revision; the ledger is otherwise confirmed accurate by the owner. The four-act structure (fix the ground / design interviews / wire together / delete) is confirmed as the right conceptual frame.

| # | Ruling | Lands in | Status |
|---|---|---|---|
| R-1 | **Thin, don't eliminate.** Only lifecycle-owned content leaves the auto-memory layer and home specs; the rest stays and keeps being served. `~/CLAUDE.md`/`~/AGENTS.md` thinned to pointers + non-lifecycle content, never deleted. All later "phase-out / retirement / empty directory" language is scoped to the lifecycle subset. | 1 · 3 · 11 · 12 · README · ledger D-5 | APPLIED to 1, README, ledger, 3 (opening, brain-isolation corollary, Step 4 rationale, DECIDED row). PENDING 11 (CLAUDE.md fate, loader question reframe), 12 (KEEP-IN-LAYER bucket; terminal state = thinned + zero dangling refs) |
| R-2 | **Shared status-atom vocabulary, two families.** One atom set in `shared/composition/src/schemas/common.ts`; each note type declares a subset (`z.enum(Atom.extract([...]))`) plus its own transitions. Families: artifact lifecycle (DRAFT/PROPOSED/ACCEPTED/SUPERSEDED…) vs work progress (PENDING/READY/IN_PROGRESS/BLOCKED/FAILED/DONE/DEFERRED/ABANDONED…), sessions a small third (IN_PROGRESS/PAUSED/DONE). Same atom = same meaning everywhere; subset, never redefine. | 5 (defines) · 7, 8, 10 (subsets) | APPLIED to 5, 7, 8, 10 |
| R-3 | **Session invariants replace the global single-active-session rule.** A session binds to exactly one plan; ≤1 active session per plan; ≤1 active owning session per part (the load-bearing clause; makes team-mode legal later). SESSION-PROTOCOL's global constraint dies with that file's supersession. | 10 (+5 for owning_session fields) | PENDING 10 |
| R-4 | **Session frontmatter carries the filterable bindings**: `started`, `ended`, `branch`, `plan` (permalink), `parts` (list), plus existing `status`/`type`/`tags`. Rule: frontmatter for filtering, Relations for traversal, and a validator checks they agree. Composition session schema must carry the keys (no silent-discard). Note `after_date` filters on index-modified time, so explicit date keys are what make date filtering real. | 10 (+5 schema) | PENDING 10 |
| R-5 | **Session sections: frontmatter + Events + Observations + Relations. Nothing else.** `## Scope` folds into frontmatter + Event 01. Optional `## State` dies — a pause is an event. Append-only after creation except frontmatter status/ended. Rationale: every other section in the corpus was plan-state duplication or protocol theater with no validator. | 10 | PENDING 10 |
| R-6 | **Cadence is immediate, per-event, same turn — enforced by a typed event enum.** Candidate types: SESSION_OPEN/PAUSE/RESUME/CLOSE, PART_TRANSITION, DISPATCH, ARTIFACT_WRITE, DECISION_LOCKED, GATE_RESULT, USER_RULING (Q+A verbatim), COMMIT, HALT, CURATION_OP, CORRECTION. Ordinal `## Event NN` headings; wall-clock timestamp (from `date`) in body. "Every 5 turns / at milestones" dies with the orchestrator's rival definition. Hook enforcement available (`invoke_session_log_guard.ts` exists in templates). Event schema lives in the composition library. | 10 (+5 schema home) | PENDING 10 |
| R-7 | **Session length: sitting-based, never phase-bound.** Pause/resume allowed (resume-don't-recreate stands). Parts bind sessions, not the reverse; research⇄decisions naturally multi-session. Size guideline: past ~N events, close and open a successor; `decompose` is the repair for existing monsters (316-event fond note is the cautionary example). N to be set in prompt 10's interview. | 10 | PENDING 10 |
| R-8 | **A `scope` phase before research, producing the PRD** (owner floated, assistant supports; converges with the already-decided PRD-first item D-12). Makes the PRD a real part with its own session and status instead of hiding inside `/plan create`. Phase set becomes: scope → research ⇄ decisions → spec → build → review → end. CONFIRM during prompt 7 review before treating as decided. | 7 · 11 (phase model) | LEANING — confirm at prompt 7 |
| R-9 | **PLAN references the PRD; never composes it.** PRD-ish sections (overview beyond one line, product scope, product risks, success criteria) live in the PRD and are referenced like ANALYSIS/ADR/SPEC notes. **Split risks by kind**: product risks → PRD; execution/sequencing risks (pre-mortem V-rows, wave gates) stay on the plan. Per-part exit criteria stay in parts. | 5 | PENDING 5 |
| R-10 | **Handoff is a flow, not a file** — plan-spawn: detect out-of-scope → `/plan create` the dep plan (starting at its scope phase) → link `depends_on`/`blocks` → mark the parent part BLOCKED with a pointer → optional worktree → return. Router + curation job. NEW work unit, currently in no prompt. | 5 (BLOCKED semantics, inter-plan deps) · 11 (routing) | PENDING 5, 11 |
| R-11 | **Parts stay. Phases stay. "Milestone" dies as rival vocabulary** (the planner agent's competing PLAN definition), not as a rename. Tasks remain artifact-level (SPEC subtree), surfaced in the plan only as derived state — no plan-level task tier. | 5 · 11 | PENDING 5, 11 |
| R-12 | **Snapshot scope: full brain + targeted rest** (all 11 ACMElabs skills, grill-me, create-skill, all brain/skills/** + rules + agents + commands + instructions, 8 home specs, memory layer, composition src; excludes node_modules/.git/coverage/dist). Prompt 1's runtime question is removed — zero questions, one map gate. **Process note: originally baked from a "not sure"; explicitly RATIFIED by the owner 2026-07-27.** | 1 | APPLIED + RATIFIED |
| R-13 | **Regrowth is routed, not amputated** (owner's option 3 + delta-sweep): the two instruction-level clauses are narrowed in place with a lifecycle-ownership routing rule; all six generation/loading settings stay untouched; prompt 12 delta-sweeps files newer than the frozen inventory. The narrowed rule must survive the prompt-10 migration of `feedback_session_protocol`. | 1 · 10 (rule survives migration) · 12 (delta-sweep) | APPLIED to 1. PENDING 10, 12 |
| R-14 | **Prompt 12 gains a KEEP-IN-LAYER bucket** in the classification, alongside the delta-sweep. Terminal state: thinned layer, zero dangling references, index accurate. | 12 | PENDING 12 |
| R-15 | **The two-step build-task model.** Each build spec task = implement → validate, looping until PASS. Implementer reads the TASK note and, via Relations, its REQ + DESIGN notes; QA receives the same refs and reads the same notes; visual implementations add chrome-devtools validation to step 2. On FAIL the orchestrator routes back **pointing at the QA note artifact** (pointer, not orchestrator-transcribed fix-brief). The two steps are always sequential; tasks parallelize per `depends_on` relations (sharing a dep ≠ sequential — only a dep *between* them is), and specs likewise. The canonical cycle file opens with this frame; a–u is its operational choreography. | 2 (frame) · 9 (step-(s) pointer semantics, wave derivation) | APPLIED to 2, 9 |
| R-16 | **The valid/invalid judgment is the QA agent's alone.** No mechanical validator gates it; the ten claim validators stay unwired **by decision** (available to prompt 9 as advisory tooling only). All "schema validator / mechanically caught" enforcement claims rewritten to the real trust chain; mutation-layer schema validation of state writes is state management and stays. Target brief model is **pull for both** agents, brief naming spec + task; shipped implementer script (push, no spec ref) reconciled by prompt 9. | 2 (rewrites) · 9 (script, advisory boundary) | APPLIED to 2, 9 |
| R-19 | **Standardized dispatch + return message templates (owner-raised; external exemplar).** The orchestrator's handoff and the specialist's return each follow a defined template. Exemplar: `~/Dev/ai-agents/.claude/agents/` — **NOT owned by Peter; an inspiration source for brain, cited as example only** — where dispatch includes *"context, constraints, expected output format, success criteria, dependencies"* with `EXPECTED OUTPUT: [format…]`, and returns are format-enforced by reject-and-redelegate (`orchestrator.md:62,141,147`). **Port the pattern, re-derive the text**: no verbatim lifting from a codebase he does not own, and his templates need Brain-note-shaped fields anyway. Align return fields with R-6's typed event enum and the `## State Changes` block. This is the parse contract that makes hook-performed state writes possible at all. | 9 (dispatch scripts, templates) · 10 (event payload alignment) | PENDING 9, 10 |
| R-20 | **Staged writer path (resolves R-17's "is it a lot?").** Evidence, correctly weighted: **one Chesterton's fence is Peter's** — brain's hook set was de-registered 2026-06-30 (`feedback_drift_detector_hook:24`); post-mortem required before hooks graduate to writer. Secondary, secondhand evidence: the external ai-agents codebase, with full hook infrastructure, chose warn-only hooks (`invoke_qa_agent_validator.py` — *"non-blocking… all errors are warnings"*, message-text-based) and checkpointing over performing (`invoke_plan_state_sync.py`) — an upstream author's choice worth understanding, not Peter's own retreat. Staging: (1) R-19 templates land; (2) day-one writer = orchestrator single-writer invoking the shipped mutation scripts; (3) hooks stage 1 = gates + checkpoints (R-18 as the warn→deny, artifact+schema upgrade of the exemplar's validator pattern); (4) hooks stage 2 = performer, contingent on 1–3 proven and the brain post-mortem done. **Lineage note for prompts 10–12, strengthened by the ownership correction:** brain pulled inspiration from ai-agents, so brain's nine phantom `.agents/SESSION-PROTOCOL.md` citations, `.agents/sessions/` paths and handoff-file references are **inherited from an external system** — debris to remove, not lost intent to restore. | 9 (staging + brain post-mortem; ai-agents hooks readable as reference) · 10, 11, 12 (fossil dispositions) | PENDING 9-12 |
| R-18 | **QA-completion gate via hook (owner-raised).** When the QA agent attempts to claim it is finished, a lifecycle hook (SubagentStop/Stop-class) checks that (1) a QA note artifact was created and (2) it is structurally valid — and **denies the completion message to the orchestrator** if either fails, logging the denial to the session note. Compatible with R-16 by construction: it gates the *deliverable*, never the *judgment*. Natural wiring point for `validateQaPassClaim` (P4-13: wire, don't rebuild); `brain/.claude/hooks/Stop/` shows the pattern has precedent in the owner's tree. Open sub-questions for prompt 9: does the implementer get a symmetric gate (State Changes returned, DoD checkboxes touched)? Who writes the denial event under R-17's writer ruling? | 9 (design + wire) · 10 (denial events in the session enum) | PENDING 9, 10 |
| R-17 | **State-writer fork — OPEN, working default: orchestrator single-writer.** Who flips PLAN/TASK statuses and appends SESSION events: (a) orchestrator single-writer [default — serializes under parallelism, matches `feedback_agents_no_autonomous_git_or_plan_writes` and the shared-tree module-only principle]; (b) workers own their bookends (impl→IN_PROGRESS first act, QA→DONE last act); (c) hybrid — workers flip the TASK note only, orchestrator mirrors; (d) **hook-based lifecycle middleware** (owner-raised) — PostToolUse/SubagentStop hooks perform or enforce the writes; most reliable because event-driven, with two caveats: the hook set was deliberately de-registered 2026-06-30 (`feedback_drift_detector_hook` — investigate why before re-registering, Chesterton's fence) and hooks lack direct MCP access (basic-memory CLI or an orchestrator-flushed queue). Decided in prompt 9; interacts with prompt 10's event-writer column (R-6). | 9 (decide) · 10 (event authorship) | PENDING 9, 10 |

| R-21 | **Pure Bun, program-wide (owner-stated).** No script provided to agents is ever authored in Python or Node — always pure Bun TS. Promotes the owner's standing memories (`feedback_pure_bun_ts_scripts`, `feedback_labs_scripts_pure_bun`, `feedback_bun_first`) to a program rule; his registered hooks already comply (`settings.json` runs all three via `bun run`). Consequences: every hook or script any prompt authors is Bun TS; the ai-agents `.py` hooks are pattern-reference only, never ported as Python; message-standard tooling lives in `shared/composition/` as Bun TS. | all prompts that author scripts (4, 5, 6, 9, 10) | STANDING |
| R-22 | **The message standard (v0.3, owner-narrowed).** Scope: **three agents only — orchestrator, implementer, qa.** Carrier = markdown + fenced JSON State block, as *convention* — **no Zod schema for now** (owner-deferred until something mechanically consumes the block: the R-18 gate or a hook performer, both open prompt-9 forks). The standard IS the agent files: templates live at the **end** of `orchestrator.md` (dispatch) / `implementer.md` / `qa.md` (returns), values defined in each. Key semantics: `status` (COMPLETE/BLOCKED/FAILED) describes the dispatch's work; `verdict` (PASS/FAIL, qa-only, R-16-sovereign) describes the implementation — `COMPLETE`+`FAIL` is the normal fail-loop signal. **Lands entirely in prompt 9**, which also reconciles the two dispatch scripts to render the orchestrator template (one definition, script-rendered). Prompt 5/7/10/11 threading, `agent-messages.ts`, and the drift canary are REMOVED from scope. Still open in prompt 9's interview: enforcement point; wider adoption. | 9 (all of it) · 10 (event mapping, informational) | v0.3 — 2 forks to prompt 9 |
| R-23 | **Brain-edit scope (owner-stated): light and specific, never wholesale.** The majority of all change lands in the skills plugin; brain-agent edits are sanctioned for targeted things like appending the standardized message templates and their values; auto-memories and home specs get specific updates only. **Re-measure against this bar during their reviews:** prompt 11's template-stripping step (TPL-BRAIN-STRIP) and prompt 10's brain session-skill retirements — both may need narrowing from "strip/retire" to "targeted edit + pointer." | 10, 11 (re-check) · all brain-touching steps | STANDING |
| R-24 | **Prompt-3 preamble de-overclaimed (owner challenge: "how do you know there are 20?").** The "twenty" was the synthesis sweep's classification (A2 + an E-5 policy sweep), never an owner-stated fact — prompt 3 now states that provenance, presents rows as reviewed routings, and instructs the runner to flag-don't-promote further candidates. The stale "phasing out entirely" language (pre-revision D-5) was purged from all four sites (opening, brain-isolation corollary, Step 4 reflect rationale, DECIDED row 1) and replaced with R-1 thin-don't-eliminate. | 3 | APPLIED |
| R-25 | **Six owner delete rulings (walkthrough 2026-07-27).** `feedback_credentials_via_skate`, `feedback_bun_coverage_bunfig_override`, `feedback_labs_scripts_pure_bun` (Labs = `/Users/peter.kloss/Labs`, owner-stated; pure-Bun directive survives as R-21), `feedback_stray_parent_dir_repo_poisons_tooling`, `feedback_gh_review_history_pagination_and_stale_approve` (dissolves the github-ops resolution question; the home CLAUDE.md's stale `github-ops` reference stays a prompt-11 item), `feedback_ask_when_auto_mode_classifier_down`. No destinations. **Deletion executes in prompt 12** (owner-chosen lane); prompt 3 carries them as `OWNER-RULED-DELETE` ledger rows with their `MEMORY.md` index lines. Prompt 3 shrinks 20 → 14 exports (5 project + 9 references); worst-case questions 3 → 1 (`ux-platform-oncall` source only). | 3 (ledger rows) · 12 (execute) | SUPERSEDED by R-27/R-28 — the content review widened deletion to 21 of 22, and execution moved in-walkthrough |
| R-27 | **Content review of every prompt-3 row (R-26 applied, 2026-07-27): 21 of 22 sources ruled DELETE, 1 ratified.** Ratified: `feedback_claude_code_markdown_first` (markdown-first authoring rule → `authoring-style.md` append). Deleted — routing round: skate-credentials, bunfig-override, labs-pure-bun, stray-parent-dir, gh-review-pagination, classifier-down; policy round: datatable-v2-lens, precommit-failing-step, oncall-1password (that skill's existing guidance stays untouched), inline-reflect-capture (verbatim duplicate of the shipped `~/REFLECT-PROTOCOL.md`, which is `~/CLAUDE.md`-referenced; the protocol itself owner-affirmed); per-item batches: phase-X-state, implementer-package-build, jira-board-fields, docs-site-rebuild, write_note-quirks, list_directory-case, config-disconnect, date-filter-semantics, leaked-cc-source, rdap-bootstrap, jira-key-frontmatter. Consequence: SKILL-002 E-1/E-3 lose their routed destination (the troubleshooting doc will never exist) — they stay in SKILL-002; prompt 7's handoff line updates at its review. | 7 · 11 · 12 | APPLIED (see R-28 for execution) |
| R-28 | **Prompt 3 dissolved; the 21 deletions executed immediately (owner: "dissolve and just delete the auto memories now").** Executed from the walkthrough session via device tools: 21 files moved to `~/.claude/memory_deleted_2026-07-27/` (Cowork cannot `rm`; owner empties the folder), 21 `MEMORY.md` index lines + the emptied `## Reference memories` header removed, post-compaction memory's two stale phase-X pointers removed (pre-empts prompt 1's re-point step — now a verify). Layer: 119 → **98 root items** (97 feedback + MEMORY.md). The markdown-first append rides in **prompt 4** (end of `authoring-style.md`, append-only, after its own line-number reads). Programme = **11 active prompts**; prompt 11's dep on 3 dropped. **Execution findings:** `~/.claude/memory/` holds ~33 per-project subdirectories (Claude Code per-project auto-memory) — the synthesis "zero subdirectories" figure is stale — plus one un-indexed root file (`feedback_subagents_use_opus.md`); prompt 12's delta-sweep must cover both. Record: `OWNER-RULED-DELETE.md`. | 1 · 4 · 11 · 12 · README | APPLIED (execution done; prompt riders PENDING) |
| R-26 | **Auto-memory content is unratified by default (owner-stated).** "Claude creates them so eagerly so I don't even know to what degree they're all things I completely agree with." Consequences: (1) migration = **destination-first re-authoring** — the memory is an input, never the artifact; "verbatim" only as a deliberate, reviewed exception; (2) two classes — **technical tool-behaviour observations** migrate as dated, provenance-marked, recorded-not-ratified observations (re-verified where a one-command check exists); **policy/procedure content** lands only as ratified or revised in the walkthrough; (3) every prompt constraint that derives solely from an auto-memory gets flagged for ratify/revise/kill during that prompt's granular review. First flag raised: `feedback_inline_reflect_capture`'s *never-ask-whether-to-capture* rule (prompt 3 Step 4, marked UNDER REVIEW — it institutionalises the eager capture itself). | all prompts · 3 (frame APPLIED) · 4–12 (flag-pass per review) | STANDING |
| R-29 | **ASK-STANDARD (owner-raised).** A standard for how `AskUserQuestion` is used — question formatting, plain-language rules, dos/don'ts, batching, option design — developed by **grilling the owner inside prompt 7's interview** (owner-chosen placement), installed wherever he chooses at that time. Interim standard until it lands: prompt 4's P4-1 plain-language paragraph (no internal jargon in question text; what breaks and what it costs, stated simply). Origin: repeated walkthrough misses — jargon-dense questions drew "I have no context for what you're asking me here." | 7 (grill + install) · all prompts (interim P4-1) | APPLIED to 7 (its W-2/W-3) |
| R-30 | **The provenance register; the walkthrough stops being a decision venue (owner-stated 2026-07-27).** Owner: assumptions about "things I was wondering about / not sure about" must be phrased as exactly that; "I don't want to interview with you here and then again with another agent"; having to read every prompt line to suss out smuggled assumptions "is a project all on its own." Consequences: (1) every prompt carries a four-tag register — **[DECIDED]** owner rulings, dated/quotable · **[WONDERING]** owner's open questions, interviewed ONCE at runtime at their natural moment, recommendations labeled as the assistant's · **[FACT]** measured reality with reproduction · **[DEFAULT]** assistant suggestions, adopted or overridden with reported (not asked) deviations; (2) runtime skills are the authority for their own mechanics — prompts stop restating them ("why are we defining this now when the create-skill skill knows how to handle it?"); (3) no more per-decision walkthrough questions — the assistant performs the provenance rewrite of prompts 5–12 autonomously and delivers each with a short digest naming its W-items; (4) prompt 4 rewritten under the register (562→~250 lines): invariants+footgun → W-1; D-14 cleanup shape → W-2; seam edits → W-3; checker mode → W-4; doc-code gaps → W-5; delegation wiring → W-6; create-skill's stage mechanics deleted from the prompt; budgets = **targets, not gates** [owner]; shared refs = **leaning, not locked** [owner]. | all prompts · 4–12 (APPLIED 2026-07-27) | STANDING — rewrite pass complete |
| R-31 | **The run contract (owner-stated 2026-07-27).** "I want the agent to go over all of this with me in pretty granular detail — while I do want it to use the ask user question tool and identify the answer(s) it recommends — I also do not want it making assumptions for me — I want it to help provide structure, provide opinions and options, surface concerns, improvements, problems, etc and help guide me through this in an extremely collaborative way." Encoded as `RUN-CONTRACT.md`: orient-first (structure map before work), step-preview-then-act (with concerns/risks/improvements and adopted [DEFAULT]s named; silence consents only to what was previewed), W-items via AskUserQuestion with labeled recommendations (once, never pre-decided), no-assumptions rule (un-decided material choices are surfaced, not inferred), checkpoint summaries per section, opinions-welcome-but-labeled, collaboration ≠ re-litigation of [DECIDED]. Overrides all "autonomous" framing; every prompt now opens with a read-the-contract instruction. **Delivery mechanics resolved with it:** nothing is uploaded — the agent starts in the skills repo root and reads the prompt, the contract, the log, and every cited file from disk; the launch line is one sentence. | RUN-CONTRACT.md · all prompts (header line APPLIED) · README | STANDING |
| R-32 | **`create-skill` is retired; `skill-creator` replaces it (owner-stated 2026-07-28, at prompt 4 runtime).** "we removed the create-skill skill and replaced it with the skill-creator skill which should b used instead." The user-level skill at `/Users/peter.kloss/.claude/skills/create-skill/` no longer exists; the successor is the `skill-creator@ACMElabs` plugin (`settings.json:269`, enabled) at `/Users/peter.kloss/Dev/ACMElabs/skill-creator/skills/skill-creator/`. Substantive deltas, not a rename: `references/authoring-style.md` + `eval-workflow.md` (2 files, ~500 lines) dissolved into **ten** references (~1,670 lines); `scripts/quick_validate.py` → `scripts/quick-validate.ts` (pure Bun, with `--standard`/`--extended` tiers); `skill-reviewer` is now an **in-plugin** agent (`skill-creator:skill-reviewer`), so the "verify it resolves before treating it as a gate" caveat is satisfied; `skill-creator@claude-plugins-official` remains `false` (`:245`/`:257`) — the ACMElabs one is the live path. **Body-size rule superseded:** the old ~450-line target (which prompt 4 called its one hard ceiling) is replaced by the successor's dual limit — **under 500 lines AND under 5,000 tokens, both measured, both reported** [owner-ruled 2026-07-28]. The old files survive read-only in the prompt-1 snapshot at `skill-snapshot/create-skill/`. **Sweep ruling:** 37 stale citation sites across 15 files; prompts **4–12 + README** are corrected in one commit before prompt 4's own work begins; **prompts 1 and 2 keep their original citations** as the record of what actually ran, each carrying a dated note that the authority changed after they executed. Two prompts held gates that had become unsatisfiable: prompt 9's `python -m scripts.quick_validate` hard gate, and prompt 12's read-back verification of an append that will now never exist. **Sweep APPLIED 2026-07-28:** prompts 4–12 + README corrected; prompts 1 and 2 carry a HISTORICAL RECORD banner instead of edits; every surviving `create-skill` mention is a deliberate historical citation (the retirement notice, the snapshot path, D-2's verbatim text, the owner's quoted words). A throwaway benchmark graph was also built and verified for D-2's empirical stages — project `kg-fixture` at `~/Desktop/kg-fixture/docs`, 10 real notes, `--references` provably complete, disk-vs-index parity 10 = 10, `--project kg-fixture` mandatory on every command. Two measured traps recorded for later prompts: `brain projects create` writes a malformed config entry (bare string where a five-field object is required) and basic-memory then refuses to load the config at all, taking every project down — `basic-memory project add` is the correct tool; and `markdownlint-cli2 --fix` corrupts these prompt files, renumbering ordered lists and stripping underscores out of `feedback_*` identifiers in nested bullets, so it must never run over them. | 4–12 · README · RUN-CONTRACT | APPLIED |
| R-33 | **Migration is not the default; deletion is a first-class outcome (owner-stated 2026-07-28).** "we are moving rules out of the auto memory. or trying to thin it down rather. That doesn't mean every rule is going to survive. And the rules that are going to survive does not mean they're going to survive exactly as they are written." First application, ruled the same turn: **`feedback_claude_code_markdown_first.md` is DELETED, not migrated** — "In this case, I think we just delete this rule." This reverses R-27/R-28's placement of the markdown-first append in prompt 4. Consequences: prompt 4's **Step 10 is dropped** along with its `## Done means` box; prompt 12 **deletes the memory outright** — no append to verify, no read-back gate, no stop condition (the instruction to halt on a missing append would otherwise block on a condition that can never be satisfied), and prompt 12's framing of it as "the one ratified survivor" of the dissolved prompt-3 export programme is corrected to *deleted by later ruling*. Standing consequence for prompts 5–12: when a prompt says migrate, **propose deletion where the rule does not earn its place** rather than assuming migration; a survivor may also be re-authored rather than carried verbatim (compounds R-26's destination-first re-authoring rule). | 4 (Step 10 dropped) · 12 (delete outright) · 5–11 (standing frame) | APPLIED to log; prompt edits IN PROGRESS |
| R-34 | **Plugin shared-code architecture — narf's shape is the standard (owner-ruled 2026-07-28).** A plugin needing code shared across skills puts that code in a **workspace package**, makes the plugin a workspace member **depending** on it, and runs a **build step that inlines it into `dist/`**; skills invoke the built output. Reference implementation: `~/Dev/ACMElabs/narf` (`packages/core` as `@narf/core`, `plugin/` as `@narf/plugin`, `plugin/build.ts`, `plugin/README.md`). **Path anchors are part of the standard**: three Claude Code variables, three distinct jobs, never conflated, resolved in exactly one module (`plugin/src/env.ts`) with dev fallbacks so scripts run outside a session — `CLAUDE_PLUGIN_ROOT` locates shipped code (read-only, replaced on update; fallback is `new URL("..", import.meta.url)`, which is file-relative and therefore immune to the working-directory problem), `CLAUDE_PLUGIN_DATA` holds plugin state (survives updates), `CLAUDE_PROJECT_DIR` is the user's project and the graph is `docs/` under it. Two prohibitions: never write state into `PLUGIN_ROOT` (an update wipes it), never put the graph in `PLUGIN_DATA` (the graph is project data). Also standardised: hold `hooks.json` / `.mcp.json` back until their layer is complete and QA'd, because a declared hook or MCP activates the moment its file appears. **The premise is Anthropic-DOCUMENTED**, not merely observed: *"Installed plugins cannot reference files outside their directory. Paths that traverse outside the plugin root (such as `../shared-utils`) will not work after installation because those external files are not copied to the cache."* **Measured evidence:** install is a real detached copy (inode-verified; the live `skills` install at `~/.claude/plugins/cache/ACMElabs/skills/0.1.0/` is 2 days stale, so source edits do NOT propagate). `${CLAUDE_PLUGIN_ROOT}` is Anthropic's own convention — every official hook uses it, always quoted, 547 files across installed marketplaces; `CLAUDE_PROJECT_DIR` 207. **No Anthropic plugin shares code across skills** (`plugin-dev`: 7 skills, 7 scripts, no shared dir), and their four Bun MCP plugins duplicate twelve helpers verbatim — `loadAccess` byte-identical in three — so the incumbent practice is duplication, i.e. the absence of a decision rather than an alternative to one. The dividing line from the wider ecosystem is clean and confirms the choice: **install step → declared dependency** (ESLint, Terraform); **no install step → bundle** (VS Code says so officially, for the same reason). **Skills-repo application scope (measured):** only `defrag`, `decompose` and `recompose` invoke `shared/composition`, across **26 sites**; the library is 103 source + 102 test files; 9 hook files reference shared code. `review`/`spec`/`build` are untouched, so no boundary conflict with prompt 4. Note the two repos are *both* currently consistent with the documented rule by different shapes — `skills` has its plugin root AT the repo root so `shared/composition` is inside it and does get copied; `narf` has `packages/core` outside `plugin/`, which is the case the docs say fails, hence its build step. | skill-creator (merge) · skills (apply) · 4 (resumes after) | STANDARD LOCKED — merge + apply PENDING |
| R-35 | **Two standards, not one (owner-ruled 2026-07-28).** The shared-code architecture (R-34) is **conditional** — only plugins that need code shared across skills. The **TypeScript/Bun code standard** is separate and applies to all plugin scripts, framed as the standard for "the scripts or the code used in those plugins", carrying the strict owner rule: **always pure Bun, never Node, never Python.** It also covers test and fixture placement. Both likely land in `skill-creator` and `create-plugin`; merge approach to be decided thoughtfully so a plugin without the shared-code requirement never pays for it. **Research findings that correct the working assumptions:** (1) `__fixtures__/` is **not** a convention — 220 `fixtures/` directories against 1 `__fixtures__/` across 18 major repos, no tooling default references the dunder form, and React nests a plain `fixtures/` *inside* a `__tests__/` dir; (2) `__tests__/` is a **React/Jest-lineage** convention, not a TypeScript one — by repo count separate `test/`|`tests/` leads **10**, `__tests__/` **3** (all React-ecosystem), co-location **2**; only Jest's `testMatch` privileges the directory name, while Vitest and Bun match on suffix alone; so the composition library's `tests/` is the majority convention, and Bun's own repo has 1,997 tests in `test/` against 3 in `__tests__/`; (3) **`node:*` imports are correct under Bun, not a compromise** — Bun's docs say *"Use `node:fs` for working with directories in Bun"*, `node:path`/`node:os` are 100%-compatible, and Bun's own scripts use `node:*` over `Bun.*` roughly 10:1 in filesystem code, so the 155 local imports need no change; (4) pure-Bun is a **house rule, not ecosystem compliance** — Anthropic's official hooks run `python3` and `bash`; (5) Bun discovers tests by **filename suffix only** (verified empirically: co-located, `__tests__/`, `tests/` and `.spec.` all collected; a suffix-less file inside `__tests__/` is not), so no layout is mechanically required and the standard is a **legibility** rule, not a correctness one. **Measured local drift:** four conventions coexisting in the skills repo — 23 co-located files, five `__tests__/` dirs, a `tests/` dir holding 124 test files, and `fixtures/` not `__fixtures__/`; running four at once is what no surveyed repo does. The specific choice is close to arbitrary; consistency is the standard worth setting, and the research recommends following the local plurality. | skill-creator + create-plugin (merge) · skills (apply) | RESEARCH DONE — placement + choice PENDING owner |
| R-36 | **Where the two standards are filed (owner-ruled 2026-07-28).** Organising principle, owner-stated: *"the create Plugin is really more about the structure of the plug-in, whereas skill creator, they're going to be code files."* So **plugin-specific material goes in `create-plugin`; code-level material goes in `skill-creator`** — four references, two per skill, which satisfies `skill-creator`'s own one-file rule in both places (`progressive-disclosure.md:61`: *"Do not create a directory for one file"*, and rule 5 sends a lone document to the skill root instead). **`create-plugin/references/`** — (1) shared-code architecture: workspace package, plugin as workspace member depending on it, build step inlining into `dist/`, skills invoking the built output; (2) path anchors: the three Claude Code variables with three distinct jobs, the `env.ts` single-resolution module with dev fallbacks, and the two prohibitions (never write state into `PLUGIN_ROOT`, never put the graph in `PLUGIN_DATA`). The anchors live here **because they are plugin-installation facts** — what survives an update, what is wiped, where the graph lives — and `env.ts` is the prescribed implementation of that fact, so it travels with the reason rather than being divorced from it; `skill-creator`'s code references name the module and point here rather than restating it. **`skill-creator/references/`** — (3) TypeScript standard: test and fixture placement; (4) pure-Bun standard: the `node:`-vs-`Bun.*` split. **Merge discipline**, per `skill-creator`'s own improvement guidance (`SKILL.md:177`, `:179`): keep it lean — the references carry the pattern and its reasoning, never a transcript of the research; and explain the why — the documented no-install-step rule is what makes the build step something other than ceremony. Body changes in `create-plugin` are minimal: a shared-code row in the Phase 2 component table and a Phase 5 bullet, each pointing at its reference. Note the pre-existing gap the evaluation found: `create-plugin` mentions `${CLAUDE_PLUGIN_ROOT}` three times (`:99`, `:147`, `:150`) but **only ever in a hooks context**, and never mentions the other two anchors at all — so skill scripts currently get no guidance. **Pure-Bun made concrete (measured):** the skills repo uses exactly **five** node builtins — `node:crypto`, `node:fs`, `node:fs/promises`, `node:os`, `node:path` — all `node:`-prefixed with **zero** bare imports. The prefix is the rule worth stating: a bare `from "path"` can be shadowed by an npm package of that name, `from "node:path"` cannot. Anthropic's own four Bun MCP plugins use the bare form, so the local codebase is the stricter of the two. | create-plugin · skill-creator · skills (apply) | FILING LOCKED — authoring PENDING |
| R-37 | **Test and fixture placement (owner-ruled 2026-07-28).** Tests live in a **`__tests__/` directory sibling to the file under test**, named `<file>.test.ts`. Fixtures live in **`fixtures/`**, NOT `__fixtures__/` — the dunder form is not a convention: 220 `fixtures/` directories against **1** `__fixtures__/` across 18 surveyed repos, no tooling default references it, and React nests a plain `fixtures/` *inside* a `__tests__/` dir. The owner's own uncertainty on this (*"I could be wrong about that"*) was well-placed. **Measured migration cost of the ruling:** `__tests__/` leaves `narf` (15 files) and `skill-creator` (9) already conformant and migrates **125 files in the skills repo** — including overriding `shared/composition/tests/`, which holds 102 of them. The alternative (`tests/` per package) would have moved only 69 files but touched all three repos and reversed the convention in the two that are currently self-consistent. **This is a legibility standard, not a correctness one, and must be written as such:** Bun discovers tests by filename suffix ALONE — verified empirically on 1.3.14 that co-located, `__tests__/`, `tests/` and `.spec.` are all collected, while a suffix-less file inside `__tests__/` is not — so no layout is mechanically required and the justification is that a reader knows where to look and a glob targets predictably. Evidence the cost of having no rule is already being paid: `package.json`'s test script hand-enumerates paths (`bun test skills/defrag skills/ingest shared`) precisely because layout is unpredictable. Note also that `__tests__/` is a **React/Jest-lineage** convention rather than a TypeScript one (by repo count: separate `test/`\|`tests/` **10**, `__tests__/` **3** — all React-ecosystem — co-location **2**), and that only Jest's `testMatch` privileges the directory name; the ruling is a deliberate house choice, not a claim about industry consensus, and the standard should say so plainly. | skill-creator (author) · skills (apply, 125 files) | LOCKED — authoring PENDING |
| R-38 | **Package public surface — explicit `exports` map, no wildcard (owner-ruled 2026-07-28, prompt 4A runtime).** Each internal package enumerates the subpaths actually imported; the `"./*": "./src/*.ts"` wildcard is **deleted**; the dead `"."` entry (which points at an `index.ts` that exists in none of the three packages) is removed or repointed at a small curated barrel. **Measured state that prompted the ruling:** all three of `packages/{models,core,cli}` declare `"exports": { ".": "./src/index.ts", "./*": "./src/*.ts" }` while **no `src/index.ts` exists in any of them** — invisible because every consumer imports a deep subpath through the wildcard and never the bare specifier, so the broken `"."` target is never exercised (tests pass, typecheck clean). 90 distinct subpaths are in use. **Enforcement is real on this runtime, verified empirically on Bun 1.3.14:** a throwaway package with an explicit map (`{".": "./src/index.ts", "./schemas/plan": "./src/schemas/plan.ts"}`) resolves the sanctioned subpath and the bare barrel, while an unsanctioned `@probe/lib/schemas/internal` fails with `Cannot find module`; switching that same package to the wildcard makes the internal file **reachable**. So the wildcard is the single line disabling an encapsulation mechanism that already works. **The fact that makes the map an enforced gate rather than documentation:** `exports` is consulted only for bare/scoped specifiers, never for relative paths — and local imports are already **48 of 53 specifier-based** (`@acmelabs/models/schemas/plan-note`), 29 of 34 in skill scripts, 19 of 74 in hooks. **Ecosystem backing:** this is Turborepo's documented "Compiled Packages" pattern for exactly this case (internal workspace package with a build step), and `date-fns` ships 741 explicitly enumerated subpath entries with zero wildcards. Node's own docs treat enumeration and patterns as both legitimate, differentiated purely by subpath count, and ship `null` targets as the documented way to carve out privates if a pattern is kept. **Why not a root barrel** (the shape the owner initially reached for, and correctly so as a convention): Vercel measured 200-800 ms import cost for large barrels and "40% faster cold boots" after eliminating them, worst "in a serverless environment, every time the app is started" — which is this repo's cold-process-per-CLI-invocation model; a Vitest barrel-plus-circular-import bug that silently breaks `vi.mock` **recurs on current versions (closed 2026)**, a correctness risk not just a perf one; and TanStack's maintainer scopes barrels to libraries and to **curated** entry points, not an auto-generated `export *` sweep of 40 modules. A generated barrel was nonetheless built and measured for comparison — 724 KB single file, 92 runtime exports, and only **1** symbol collision across 184 exported symbols (`DoDClaimResult`, the same `ClaimResult` alias declared twice in `validators/types.ts` and `validators/task-claim-validator.ts`), so a barrel was viable and was rejected on cost, not feasibility. Cold start via barrel 40-50 ms vs 30 ms via deep import. **Latent defect to repair alongside:** the restructure commit `a1c8f9c` dropped `schemas/index.ts` from 9 exports to 3 — the six schema modules moved to `packages/core/src/schemas/` correctly and their exports live in core's barrel, so no capability was lost, but the file was left holding **7 orphaned doc comments with no export statement following them** and a header claiming its symbols were "checked for collisions across all sixteen schema modules" while it exports three. | skills (apply) · skill-creator (write back to `shared-code-architecture.md`) | LOCKED — authoring PENDING |
| R-39 | **Integration tests live at the package root (owner-ruled 2026-07-28, prompt 4A runtime).** R-37 places tests in `__tests__/` **beside the file under test**, which resolves cleanly for unit tests and not at all for tests that have no single subject. Measured: of 144 test files, **103** match a source module (58 by exact or suffix-stripped name, 45 already co-located) and **41 do not** — they are integration tests spanning several modules at once (`round-trip.test.ts` imports 3 `@acmelabs` modules, `frontmatter-invertibility.test.ts` and `mutation-invariants.test.ts` import 6 each, against 1-3 for a typical unit test). Ruling: **unit tests go to `src/<dir>/__tests__/<file>.test.ts` beside their subject; integration tests go to `packages/<pkg>/__tests__/` at the package root.** Both satisfy R-37's directory name, and the location carries the meaning — a test beside a file tests that file, a test at the package root tests the package. The rejected alternatives were a single flat `src/__tests__/` per package (satisfies the directory name but abandons the beside-the-subject half of R-37, which is the legibility the ruling was buying) and leaving the 41 in the existing `tests/` directories (smallest diff, but leaves three conventions coexisting, which is the drift R-37 exists to end). **Migration mechanics to respect:** a test moving one level deeper changes its relative import depth, and cross-package imports here are already 231 specifier-based against 4 relative — both of the relative ones point at test helpers under `_helpers/`, so the helper directory moves with its tests. | skills (apply, 144 files) · skill-creator (author alongside R-37) | LOCKED — migration IN PROGRESS |
| R-40 | **[PARTLY SUPERSEDED BY R-44, 2026-07-29 — `curate` is dropped: not built, not adopted as a name, not deferred. Every clause below naming `curate` as the skill or assigning prompt 6 to build it is void; read `defrag` as the skill throughout. The capability-vs-policy split is what survives.]** **Curation is continuous and human-in-the-loop, not a scheduled sweep; one skill, not two (owner-ruled 2026-07-28, prompt 4A runtime).** Owner: *"I don't think there needs to be two separate skills that do the exact same thing, but are just run at different times"*, and on timing: *"even though right now it kind of frames it as something that should be scheduled, happens autonomously during like a scheduled cron job, in actuality it should be happening like very actively way, way more in those human-in-the-loop moments."* **The current framing is inverted.** `defrag`'s own description reads *"Periodic curator skill… report-only mode for cron scheduling"* with six cron/schedule references in its body, which places curation where nobody is watching. It belongs where the graph is growing fastest: the research⇄decisions phase, where notes are written every few minutes, the same scope is updated several times running, and a note outgrows itself mid-conversation. A scheduled sweep is a backstop for what the continuous path missed, not the primary mode. **Consequence for the skill set:** ONE skill, the facade over `decompose` and `recompose` — *"it should be the router, or whatever you want to call it, for decompose and recompose… the thing that makes that happen, and the user should not have to think about it."* Name is `curate` [assistant-adopted, owner-indifferent: *"I'm not really sure I care which one it is, defrag or curate. It should not be both"*]; "defrag" names a periodic batch operation and so carries the model being rejected. **The behaviour is larger than routing.** Memory-first means search before writing, append to the right existing note rather than creating a new one, split when that note outgrows itself, and place the new content where it belongs rather than at the bottom — *"it's not just going to be appended onto the bottom, it's going to be put somewhere where it makes the most sense."* That last part is **content placement**, and nothing implements it today: `classify` (`plugin/skills/defrag/scripts/audit.ts:144`) sorts whole notes into split/merge/stale/structural buckets and says nothing about where content goes inside one. **Sequencing [owner-chosen]:** record now, build in prompt 6. Prompt 4 stays inside its documentation-refactor boundary and does three things — the W-3 seam edits so the classifier is reachable from a before-write caller, no "periodic"/"cron" framing in the new spine, and no second skill. Prompt 6 builds the single continuous skill, the placement behaviour, and the rename if it is taken. Prompt 6's current framing of `curate` as *"defrag's pre-write sibling"* is superseded by this ruling. **AMENDED same session — the frequency policy does NOT live in curate.** Owner: *"I'm not even sure that it's actually the curation note that has all of that definition of how frequently it should be happening… I think the curate skill just explains that it has the ability to be given many different ways of restructuring an existing note into multiple notes, new content into an existing note. It should just know how to handle that when it gets told, however it gets told."* And: *"that frequency I'm talking about probably belongs in the memory skill and memory agent, and they should be the ones leveraging that very flexible curate skill to be dynamically growing the data graph."* So the split is **capability vs policy**: `curate` knows HOW to restructure — split this note, merge these, place this content in that note at the right position — and carries no opinion on when or how often; the **memory skill and memory agent** own WHEN, which is search-before-writing, recognising the same scope updated repeatedly, noticing a note outgrow itself, and calling `curate` accordingly [owner-ruled]. This supersedes the paragraph above insofar as it read the continuous-timing rule into the skill itself. It also diagnoses the original defect more precisely than "cron is the wrong schedule": a capability should carry **no** schedule, so cron was one caller's policy leaking into the thing it calls, and replacing it with "continuously, during research" would have repeated the same mistake with a better schedule. Consequence: prompt 4 does the seam and keeps schedule language out of the spine; prompt 6 builds `curate` as a pure capability; the frequency policy belongs to the memory skill and memory agent's own prompt, not to 4, 5 or 6. | 4 (seam + spine framing) · 6 (build curate as capability) · memory skill + agent (own the frequency policy) | LOCKED — prompt 4 scope IN PROGRESS |
| R-41 | **`/plan` takes a position, never a verb (owner-ruled 2026-07-29).** Owner: *"the only argument the plan should take is the plan name followed by the phase name or section name followed by the part name, and it should just know that it's literally just drilling into the plan. That's all it needs to do. You're either not passing anything and creating a new plan, or you're instantiating a plan and telling it where to go exactly."* The invocation shape is therefore `/plan` (create) · `/plan PLAN-NNN` (open, auto-route to next-ready) · `/plan PLAN-NNN <phase>` · `/plan PLAN-NNN <phase> <part>` — a coordinate of increasing depth, no flags. **Removed as modes:** `create` (it is the no-argument case, not a verb), `--split`, `--scope`, `--migrate`, and the `--part` flag (it becomes the third path segment). **The diagnosis, owner-stated:** *"the plan is not for doing splitting merging, it's for creating updating the plan status."* `--split` was a verb smuggled into a locator, which is why `continue` and `continue-part` were ever two modes — they are the same act at two depths. Supersedes the six-mode table at `plugin/skills/plan/SKILL.md:14-24` and the trigger phrases in its frontmatter `description`. Note the boundary: D-18 removed `--split` on the grounds that curation is that job; R-41 is broader — it removes every verb, including the two modes D-18 left alone. | 5 (apply to `plan`; the 18 halt sites) | LOCKED |
| R-42 | **Nothing halts a session at the `plan` skill to do note surgery (owner-ruled 2026-07-29).** Owner: *"if you want to do complex notes splitting, merging, etc., you shouldn't be calling the plan skill for that… I don't think it should be stopping anybody at the plan skill."* So the 18 split-naming lines across `research` (9) and `decisions` (9) do not get a replacement that routes to a `plan` mode. Where a halt genuinely needs note restructuring it names `decompose` / `recompose` directly, which are skills in their own right. **The `decisions` case is code, not prose:** `pre-author-composite-gate.md:3` BLOCKS composite-ADR authoring and makes `/plan --split` the only exit, so a hard gate currently has no legal way forward; whatever replaces it is a real dispatch, not a reworded sentence. **Why the old text was worse than unhelpful:** `scope-evaluation-and-split.md` is 260 lines asserting *"content preservation is absolute… zero orphans, zero duplicates, zero compression"* (`:9`) whose entire mechanism is an eyeballed checkbox, *"Cohesion analysis complete: source items clustered by architectural coherence"* (`:111`) — zero mentions of line ranges, byte accounting or hashing in the file. `decompose` enforces exactly that guarantee for real: per-cluster line ranges, a byte-accountability proof that the partition reconstructs the source, a SHA-256 round trip, and nothing renamed on mismatch. The sketch was still being recommended over the shipped implementation. This closes prompt 5's W-3 and both halves of its W-4; none of that prompt's authored options match, so it is not to re-ask them. | 5 (W-3 + W-4 closed) | LOCKED |
| R-43 | **`defrag` ships as-is; there was never a `defrag` problem (owner-ruled 2026-07-29).** Owner, on learning the audit is real and only the final hand-off prints: *"so it does work when you invoke defrag directly. So we should just ship it. It should not be part of plan. I almost don't think there's a problem here."* Measured: the audit is genuine — it reads every note under `docs/**`, measures each against structural thresholds, and groups candidates into split / merge / stale / structural-fix; only the last step prints the dispatch instead of invoking `decompose` (`plugin/skills/defrag/scripts/defrag.ts:57-66`, *"printed dispatch"*, returned as `status: "ok"`). In a Claude Code session that gap is cosmetic: the operator reads the dispatch and invokes `decompose`. The skill therefore ships, keeps its three shared-reference pointers from `decompose` and `recompose` intact, and is **not** wired into any `plan` mode. **Scope correction this ruling forces:** `defrag` was only ever implicated because the assistant proposed routing the halt sites to it; R-42 removes those halts, so the question dissolves. Wiring the real dispatch stays prompt 4's W-6 — a known gap, not a shipping blocker. **The name is settled, not deferred** — see R-44: `curate` is dropped outright, so `defrag` is the name and no rename is pending. | 4 (W-6 wiring) | LOCKED |
| R-44 | **`curate` is dropped. It is not built, not renamed to, not deferred (owner-ruled 2026-07-29).** Owner: *"we're not implementing curate. Forget curate. Update the note, remove curate. We don't need it."* And earlier the same session: *"we didn't implement curate, so you don't even need to say decompose and recompose. You could just say defrag."* **This supersedes R-40 on the name and on the build.** R-40 adopted `curate` as the name and assigned prompt 6 to build it; both are void. The skill is `defrag`, it is the name, and nothing is pending on it — R-43's "name deferred" is closed by this ruling, not left open. The three real skills are `defrag` (find candidates), `decompose` (split), `recompose` (merge). No fourth. **What survives from R-40:** the capability-vs-policy split, which was always the substance — a restructuring capability carries no schedule, and the memory skill and memory agent own when to call it. That binds on `defrag` as it stands. **What dies with the word:** the rename, the second skill, prompt 6's "build `curate`" scope, and its framing of `curate` as *"defrag's pre-write sibling."* Prompt 6 inherits `defrag` and may add the content-placement behaviour R-40 identified as genuinely missing (`classify` at `plugin/skills/defrag/scripts/audit.ts:144` sorts whole notes and says nothing about where content lands inside one) — as work on the existing skill, under its existing name. | 6 (placement behaviour on `defrag`; no new skill, no rename) | LOCKED — supersedes R-40 on name + build |
| R-45 | **`PLAN-002-task-breakdown.md` is left alone; it was never a plan (owner-ruled 2026-07-29).** Asked what to do about the one file of seven that cannot parse as a plan, the owner's answer questioned the premise: *"the fact that it has tasks in there makes me feel like that's not correct for a plan either, I don't think. I'm not that keen to change it. Why do we need to do this? What's wrong?"* Correct on both counts. **Nothing is wrong** — no parser reads the file, nothing depends on it validating, and it does its job as the per-SPEC task tally and wave plan it is. The only thing that wanted changing was a target that said "all seven parse," which is scorekeeping rather than a defect. And converting it into a plan would make it *more* wrong, not less: its content is a task tier, which R-11 keeps out of plans entirely. Measured state, for the record: 97 lines, `type: note`, title `PLAN-002 Task Breakdown` with no colon, no Scope/Objectives/parts/status, one relation (`part_of` the real PLAN-002). **Consequence:** the parser's success target is **six of seven**, with this file recorded as a non-plan rather than a failure; the plan schema is never widened to admit it. This closes prompt 5's W-2, and rejects all three of its authored options — including the assistant's recommendation to re-type or rename it, which would have been change for a scoreboard's sake. | 5 (W-2 closed) | LOCKED |
| R-45a | **R-45 applies to `PLAN-002-composition-tooling-follow-up-register.md` as well (applied 2026-07-29, prompt 5 runtime).** A second file in this repo's own `docs/planning/` matches R-45's pattern exactly and is left alone on the same reasoning. Measured: `## Context` + `## Register` + Observations + Relations; **zero** H3 parts and zero `Substatus` fields; frontmatter lacks `complexity_tier` and `branches`. It is a follow-up register (FU-7..FU-14) filed under a plan's filename, not a plan — nothing reads it with a parser and nothing depends on it validating. Widening the plan schema to admit it would cost every real plan its structural protection, and adding the missing frontmatter would make it *claim* to be a plan it is not. Recorded rather than re-asked, because the owner already ruled the principle: a file that was never a plan is not a defect, and a target that says "all of them parse" is scorekeeping. **Consequence:** the parser's success set is the files that ARE plans; this one and `PLAN-002-task-breakdown.md` are counted as non-plans, not failures. | 5 (applied) | LOCKED |
| R-46 | **The PLAN's status and sequence views are GENERATED, both of them (owner-ruled 2026-08-02, prompt 5A runtime — closes W-1).** Owner chose "Generate both": a phase-status table and a sequence diagram, computed from the parts on every render, plain styling, *"Nothing to maintain by hand."* **This closes prompt 5's W-1**, which was never about whose shape wins — the framing the handoff correctly forbade. The real finding: the design already said these two sections *"stay genuinely derived"* (prompt 5 step 3, non-negotiable item 3) and **nothing derives them**. Measured at ruling time: `renderProgressDashboard` no longer exists in the tree; `renderMermaid` exists at `packages/models/src/renderers/mermaid.ts:62` with **zero production callers** (only its own definition matches); `wave` appears nowhere in the plan schema. So the sections are dropped on render, nothing regenerates them, and the design assumed generation — a three-way gap, not a decision to reverse. **What this preserves:** REQ-012-SPEC-007 (ACCEPTED, all four ACs `[x]`) and ADR-003 D-6/D-10/D-11 stand unreversed; `plan-001-migration.test.ts` keeps passing; the owner's hand-authored dashboards are superseded by generated equivalents rather than either destroyed or reinstated. **What it costs, stated when offered and accepted:** the generated diagram is plainer than the hand-drawn ones — the custom theming and the Wave 0..3 subgraph grouping in `PLAN-010` are not reproduced. **Cheaper than first presented:** parts already carry `depends_on` and `renderMermaid` already accepts a parts list with phase grouping, so this is wiring an existing renderer, not new modelling — the assistant's initial "needs schema work for waves" framing was corrected in the same turn. Consequence for the audit: Decision Log / Progress Log remain a *separate* question, and any ruling keeping them is a reversal of REQ-012-SPEC-007 that must be disclosed as one. | 5 (W-1 closed) · 5A (implement) | LOCKED |
| R-47 | **The PLAN template is not designed until the SESSION model exists; prompt 5A's step 14 stops at the audit (owner-ruled 2026-08-02).** Owner chose "Stop and hand forward" on learning that **prompts 6-12 have not run.** Three markers prove the state, measured at ruling time: `AdrNoteStatusEnum` is `["PROPOSED","ACCEPTED","DEPRECATED","SUPERSEDED"]` with **no DRAFT** (prompt 7 step 5 was to add it); `SessionNoteSchema` still carries `scope` and `bound_plans` as body sections against R-5's four-sections-only ruling (prompt 10's lane); and `main`'s newest commit is prompt 4A's record. **Why this blocks the template rather than merely complicating it:** the owner's own documentation defines PLAN and SESSION as a *pair* — *"Two living documents anchor everything… Because the PLAN holds status and the SESSION holds history, a fresh chat reads just those two"* — and ADR-003 D-10 routes Decision Log and Progress Log **to SESSION events**, a container whose shape R-5/R-6 redefine and prompt 10 authors. Designing one half of a defined pair against the other half's unbuilt form is the same failure shape this run already reverted three times (a rule right, a placement wrong). Two further unrun inputs would reshape it: prompt 7's W-1 may add a **`scope` phase** before research (R-8, LEANING), changing the phase set the generated Phase Status table renders; and prompt 9 rules whether waves are **derived from the TASK graph** and where the derived plan lives, which is the direct input to the `## Sequence` section R-46 just locked. **What this run still delivers:** R-46; the corrected audit as findings; a handoff naming what 7/9/10 must settle; the cold read. **What defers:** the proposed template and the re-render against a real PLAN — prompt 5's `## Done means` boxes for both are carried forward unticked, not silently dropped. **Second finding, owner-raised and accepted:** the audit measured section *presence* across eleven documents and never tested the definition the documentation sets — *"the source of truth for what we're building and where we are"*, verified by **stateless resume**. On that test the repo's own converted PLAN fails at 3,429 lines, and the remedy is a shape, not a bucket list. The successor prompt states the test as the acceptance criterion. | 5A (step 14 stops) · 10 (owns SESSION) · then the pair | LOCKED |
| — | **Process rule (owner-stated): "not sure" never becomes "decided in review."** Unsure answers are either resolved by discussion in the walkthrough or preserved as interview questions in the owning prompt; working defaults are permitted only where mechanically required and only at status quo. R-12 (snapshot scope) is the one prior violation — flagged for ratify-or-revert. | process | STANDING |

## Ripple checklist by prompt — R-30 rewrite pass APPLIED 2026-07-27

- **Prompts 1–2**: walked line-by-line with the owner pre-register; content carries his rulings without the four tags. Retag on request only. (Prompt 1's post-compaction re-point step is now a verify — the fix landed in-walkthrough, R-28.)
- **Prompt 3**: dissolved (R-28); tombstone + `OWNER-RULED-DELETE.md`.
- **Prompt 4**: DONE — template of the register; W-1…W-6; carries the R-27/R-28 markdown-first append as its final step.
- **Prompt 5**: DONE — R-2, R-4 (schema side), R-6 (schema home), R-9, R-10, R-11 installed as [DECIDED]; W-1…W-5 (PLAN shape winner, PLAN-002 disposition, halt replacement text, `--scope`/`migrate` fates, part-id grammar).
- **Prompt 6**: DONE — D-22/D-6/D-7 + R-13/R-26 installed; classifier-seam import conditional on prompt 4's W-3; W-1…W-8 incl. the brain-side strip converted to W-7 per R-23's re-measure flag.
- **Prompt 7**: DONE — D-3/D-7…D-13 installed; W-1 = scope phase (R-8 leaning, confirmed here once); W-2/W-3 = ASK-STANDARD grilling + install home (R-29); W-13 = E-1/E-3 disposition (R-27, the troubleshooting doc will never exist); W-4…W-12 = ledger-open items deduped against rulings.
- **Prompt 8**: DONE — D-20/D-21 + R-2 subsets + R-15/R-16 consumption-context installed; W-1…W-9 (spec status machine, deferred_rationale, prose-hatch shape, hand-back target, re-entry cluster, CR entry/delta/provenance, E-2 landing, validators-vs-notes).
- **Prompt 9**: DONE — R-15/R-16/R-18/R-19/R-20/R-21/R-22 v0.3/R-23/D-13 installed as decided items 1–15; both dispatch scripts reconciled to render the dispatch template (closes the QA-brief asymmetry); Step-0 map gains the R-26 migration digest for the 23 memories it moves; W-1…W-10 (NO-batching scope, cap source, concurrency ceiling, Q15 module-only rule, Figma depth, wave failure policy, state writer, implementer gate, enforcement point, wider adoption).
- **Prompt 10**: DONE — R-3/R-4/R-5/R-6/R-7 installed as the session model; R-13 survival clause; R-20 debris removals; consumes prompt 9's writer ruling for event authorship and denial events (R-17/R-18); W-1…W-7 (definition home, root/filename, SS-2 contract, one-protocol-or-two, the number N, enum additions confirm-only, brain-side scope per R-23).
- **Prompt 11**: DONE — P4-11 precedence + R-1 thinning + R-13 flags-stay-ON + R-3 supersession installed; R-27/R-28 riders added (verify `~/REFLECT-PROTOCOL.md` + `~/CLAUDE.md` pointer, apply the R-13 routing note to that doc's `skillbook → feedback_*.md` persistence hops, remove the stale `github-ops` ref, dep on 3 dropped); consumes prompt 7's phase model and prompt 9's writer ruling; W-1…W-8 incl. TPL-BRAIN-STRIP converted to W-6 per R-23.
- **Prompt 12**: DONE — R-28 ground truth reconciled (the 21 are already deleted: consume `OWNER-RULED-DELETE.md`, never re-delete; delta-sweep covers the ~33 per-project subdirectories and un-indexed files; markdown-first cleaned MIGRATED only after prompt 4's append verifies by read-back; offer final removal of `~/.claude/memory_deleted_2026-07-27/`); R-1/R-14 terminal state (thinned, never emptied); W-1 = how the ~97 get reviewed (per-item / clustered / veto-by-exception; silent bulk classification rejected per R-26/R-27), W-2 = the nine no-destination memories, W-3 = live wikilinks in audit records, W-4 = the holding folder.
