# Prompt 9 — Build autonomy: derived waves, one iteration cap, visual QA, and the agent message standard

_Run after prompts 2, 5 and 8. Paste everything below into a fresh Claude Code conversation._

---

**Execution contract (R-31): before doing anything else, read `scratch/prompts-v2/RUN-CONTRACT.md` — it sits beside this file — and run this entire prompt in its collaborative mode: granular, guided, opinions labeled, no assumptions. It overrides any "autonomous" framing below.**

## How to read this prompt — the provenance register

Every substantive statement below carries one of four tags. This is the contract that keeps Peter's decisions his, and the assistant's analysis labeled as analysis:

- **[DECIDED]** — Peter decided it, dated, in his own words where quotable. Do not re-litigate and do not re-ask.
- **[WONDERING]** — Peter's open questions. These ARE the interview: raise each at its marked moment via `AskUserQuestion`, exactly once, with the full relevant text shown. Attached recommendations are the assistant's, never his. Nothing in this register is pre-decided, however confident the recommendation sounds.
- **[FACT]** — measured repo or tool reality, with reproduction where it matters. Verify before relying on one; report mismatches as drift, not as license to improvise.
- **[DEFAULT]** — assistant design suggestions. Adopt or improve at runtime; deviations are *reported* with the measurement that justified them, not asked about.

If anything below reads as [DECIDED] but smells like an assumption, stop and surface it. That is a defect in this prompt, not a constraint on you.

## The mission

Make the autonomous build phase real in `/Users/peter.kloss/Dev/ACMElabs/skills`, and install the agent message standard. Waves derive from the TASK graph instead of being transcribed by hand. The implement→QA loop gets one iteration cap. The shared-tree and visual-QA disciplines — today auto-memories — become `build` reference files. `build` gains the phase-start authoring analysis `spec` already has. And the three build-loop agents get their standardized dispatch/return templates.

**Read the whole prompt before touching a file.** Most of what follows is already true in the repository and needs *documenting*, with the competing prose deleted. If you find yourself designing something, stop and check whether it already ships (P4-13).

**HUMAN ATTENTION: one map gate, ten W-items at their natural moments, the findings review, plus skill-creator's own gates on the skills touched.**

**Working spec on disk:** `scratch/prompts-v2/MESSAGE-STANDARD-DRAFT.md` (v0.3) is the ratified working spec for the message standard. Once Step 6 lands the templates, the agent files are canonical and that draft becomes a design record. [DECIDED — R-22]

## Standing rules (programme-wide)

- **P4-1 — Every question goes through `AskUserQuestion`.** Max **4 authored options**; "Other" is auto-appended, so never author one, and say in the question text that Peter can type his own answer there. Questions in **plain language** — no internal jargon or stage numbers; what breaks and what it costs. (ASK-STANDARD is being developed in prompt 7's interview [DECIDED — R-29]; until it lands, this paragraph is the standard.)
- **P4-2 — One decision per question, one question in flight.** A multiselect decomposing a single decision is not a bundle.
- **P4-3 — Author the moment it locks.**
- **P4-4 — Independent evaluation mandate on every skill touched (D-2).** Cold-read, ranked findings, recommended action + one-line rationale each, nothing applied without approval.
- **P4-5 — The full `skill-creator` lifecycle runs for every skill touched, under the skill's own procedure** [DECIDED — D-2 + R-30: create-skill is the runtime authority for skill-authoring mechanics; this prompt does not restate them]. Repo facts the skill cannot know: the prompt-1 snapshot is the baseline — find it, point evals at it, never re-baseline; `skill-creator@claude-plugins-official` (`settings.json:245`) and `plugin-eval@claude-code-workflows` (`:241`) are `false`, while `skill-creator@ACMElabs` is `true` (`:269`) and is the live path; `grill-me` is user-level under `/Users/peter.kloss/.claude/skills/`, whereas `skill-creator` is a plugin at `/Users/peter.kloss/Dev/ACMElabs/skill-creator/skills/skill-creator/`; `skill-reviewer` now ships in-plugin as `skill-creator:skill-reviewer` and resolves, so the fallback branch is moot. **The predecessor `create-skill` was deleted 2026-07-28 (R-32)** — descriptions are now capability-first with a deliverable clause and a same-domain exclusion (pushiness is a measured defect, inverting the old advice), and body size is a dual limit of under 500 lines AND under 5,000 tokens, both reported. **Hard gate kept:** `bun scripts/quick-validate.ts <skill-dir> --extended` passes for every SKILL.md changed, and the description-triggering benchmark shows no regression against the prompt-1 baseline.
- **P4-6 — Binary tool rule.** `docs/**` → Brain MCP; everything else → `Read`/`Edit`/`Write` (Brain MCP forbidden). Nearly everything here is skill text and `.ts`.
- **P4-7 — Git.** Verify branch first; one branch; coherent commits (≤5 files or one logical change); unmerged; no push; no `--no-verify`; no AI attribution in any commit message.
- **P4-8 — Update cost is work, not an argument (D-19).**
- **P4-9 — Nothing deleted in bulk; nothing deleted before its destination is live and verified** (read-back or exercise). This prompt deletes nothing from disk; the migrated memory sources stay put — prompt 12 owns the layer. `Bash(rm:*)` is on the ask list (`settings.json:158`); if you find yourself reaching for `rm`, stop.
- **P4-10 — Read before designing, and say what was read. Do not sample.**
- **P4-11 — Precedence:** skills plugin > brain plugin > home specs > auto-memories.
- **P4-12 — Fail closed; never "document a rationale and proceed."** The corpus's 27 such branches are A4 F-37's enumeration (a register of branch sites, not a phrase count — cite the register, never a grep). The counter-models are in this very skill: `build/SKILL.md:257`, `build/references/exit-gates.md:3` — *"'I'll fix in review' is NOT acceptable rationale."*
- **P4-13 — Do not build what already ships.** In play here: `end/scripts/run-pre-flight.ts`, `validateIntegrityFloor` (`core/validate.ts:32`), and the dispatch-brief inversion (`build/scripts/dispatch-implementer.ts:44-73`, `dispatch-qa.ts:48-88`) — all with **zero non-test, non-hook callers** (phrase it exactly that way; `ANALYSIS-005:61-62`). Wire, don't rebuild.
- **P4-14 — No sentence claiming a schema/validator/mutation "enforces" anything against production data** unless prompt 5's parser fix is verified to have landed. The composition suite was 458 synthetic-fixture tests at last record (`QA-030…:35, :38`); the build-part-free fixture note at `plan/scripts/render-plan-note.test.ts:10-11` is a plan-skill script test, not a composition test. Verify prompt 5 landed; add a build-part fixture if it did not.
- **`allowed-tools` pre-approves; it never restricts** [FACT]. Never write that something is blocked by a missing grant.
- **Line numbers** were taken against a corpus snapshot; open each citation before acting on it; where one does not resolve, say so.

## Decided ground — do not re-open  [DECIDED]

| # | Decision | Source |
|---|---|---|
| 1 | The implement→validate loop is **invariant, never written into a plan**: every TASK is implemented by `brain:🧠-implementer`, then validated by `brain:🧠-qa`; on FAIL the orchestrator routes back **pointing at the QA note artifact** — a pointer, never an orchestrator-transcribed fix-brief; iterate until PASS | R-15 (D-15a refined) |
| 2 | Pull-model briefs for **both** agents: the implementer is briefed with the TASK ref and reads the TASK note plus, via its Relations, the REQ and DESIGN notes; **QA receives the same refs and reads the same notes.** A target, not the shipped state — see the dispatch asymmetry fact below | R-15/R-16 (D-15b) |
| 3 | Visual work adds Chrome DevTools MCP validation to the QA step | R-15 (D-15c) |
| 4 | The PLAN's build section defines **sequencing only**; waves are **derived, not authored**; tasks parallelize per `depends_on` — sharing a dep ≠ sequential, only a dep *between* two tasks is | D-15d/e · R-15 |
| 5 | "Visual" is **derived** from a Figma reference or visual acceptance criteria reachable through the TASK's `implements` chain — **never a separate flag** | Q21 (ledger) |
| 6 | **Figma is the UX authority.** A note drifted from Figma is a defect to ERRATA; a PASS against a drifted note is invalid | SS-79 |
| 7 | The build section is authored at build-phase start by an analysis over the completed SPECs, maximising parallelism | D-16 |
| 8 | **The valid/invalid judgment is the QA agent's alone.** No mechanical validator gates it; the ten claim validators stay unwired **by decision** — available as advisory tooling only. Mutation-layer schema validation of state writes is state management and stays | R-16 |
| 9 | **The message standard v0.3 lands here, entirely**: three agents only (orchestrator / implementer / qa); carrier = markdown sections + one fenced JSON State block, as convention; **no Zod schema for now** (deferred until something mechanically consumes the block); templates live at the **end of each agent's own .md file** — dispatch closes `orchestrator.md`, returns close `implementer.md` and `qa.md`; `status` ∈ COMPLETE/BLOCKED/FAILED describes the dispatch's work, `verdict` ∈ PASS/FAIL (qa-only, R-16-sovereign) describes the implementation, and `COMPLETE`+`FAIL` is the normal fail-loop signal; both dispatch scripts are reconciled to **render** the orchestrator's dispatch template — one definition, script-rendered, never a second copy that drifts | R-22 |
| 10 | The dispatch/return pattern is **ported from an external exemplar, never lifted**: ai-agents is not Peter's codebase; re-derive all text; fields come from his own system | R-19 |
| 11 | **The staged writer path**: (1) R-22 templates land → (2) day-one writer = orchestrator single-writer invoking the shipped mutation scripts → (3) hooks stage 1 = gates + checkpoints → (4) hooks stage 2 = performer, contingent on 1–3 proven **and** the brain hook post-mortem (the hook set was de-registered 2026-06-30 — `feedback_drift_detector_hook:24`; investigating why is part of this prompt, Chesterton's fence) | R-20 |
| 12 | **A QA-completion deny gate exists by design** (owner-raised): when QA claims completion, a lifecycle hook checks (1) a QA note artifact exists and (2) it is structurally valid, and **denies the completion message** if either fails, logging the denial to the session note. It gates the *deliverable*, never the *judgment* — compatible with R-16 by construction. Natural wiring point: `validateQaPassClaim` (wire, don't rebuild — P4-13); precedent exists at `brain/.claude/hooks/Stop/` | R-18 |
| 13 | Brain-agent edits in this prompt are **exactly the three template appends** — append a closing section to `orchestrator.md`, `implementer.md`, `qa.md`, change nothing else in them | R-23 · R-22 |
| 14 | Any tooling authored here is **pure Bun** — never Python or Node | R-21 |
| 15 | Skill templates beat brain-agent templates | D-13 |

## Already shipped — document it, delete the prose that says otherwise  [FACT]

- `build` already resolves ordering from the TASK graph at execution time; the PLAN is not consulted (`implementation-phase-workflow.md:93`; `build/SKILL.md:218, :220`). The `depends_on` source is stated in exactly one place.
- The dispatch-brief inversion is **built, tested and shipping on the implementer side**: `dispatch-implementer.ts:44-73` renders from `{taskRef, taskContent}` (`:17-20`), no `planPath`, no `partId` (test: `dispatch-implementer.test.ts:21`). **`dispatch-qa.ts` is not symmetric**: `parseArgs` returns `{taskRef, reqRefs}` (`:45`), `renderQaBrief` (`:48-88`) never receives `taskContent`, compensating with the read-the-subtree instruction at `:74`. Decided item 2 is a target the QA script does not yet meet — Step 6's reconciliation closes exactly this gap. 57 build tests pass across five `build/scripts/*.test.ts` files.
- The iteration cap already exists in the schema: `failed_iterations: z.number().int().min(0).max(3).default(0)` (`schemas/plan-note.ts:49`).
- **The PLAN note is not the dispatch-brief carrier — it is not, and never was.** `renderBuildWorkflowItem` (`renderers/plan-note.ts:190-203`) emits eight status bullets and zero instruction content; `BuildWorkflowItemSchema` (`schemas/plan-note.ts:41-52`) has no free-text field; `renderPlanNote` is a pure function that cannot read a TASK note even in principle. The "instruction blocks" language traces to an unimplemented X.D.2 design goal. `build_workflow_items` stays what it is: a derived per-TASK progress rollup.
- **Verify prompt 2 landed** (one a–u cycle definition + six pointers; `build/SKILL.md:63`'s "verbatim from PLAN" claim gone). **Verify prompt 5 landed** (parser reads real PLANs; renderer no longer destroys phase H2s) before running `render-plan-note.ts` against a real PLAN. **Verify prompt 8 fixed `TPL-TASK-RELATIONS`** (`brain/agents/spec-generator.md:304-308` emitted no `part_of [[SPEC-NNN]]`, no `implements [[REQ-NNN]]` — specs from that template are unschedulable and item 2's briefing model is unexpressible). If any of the three did not land, stop and tell Peter — do not redo their work here.
- **Bounded autonomy**: `plan/references/auto-routing.md:70` forbids auto-invoking the next phase skill; `end` is not auto-routable (`:34, :36`). Autonomous build holds *within* a build part. The five halts autonomy never waives (`feedback_build_phase_autonomous_no_checkin.md:18`, surviving verbatim into the skill body): a genuine spec-vs-render-truth contradiction; a spec-invalidating pre-mortem risk; an exit-gate CRITICAL_FAIL needing a scope decision; a fix loop exceeding the cap; any AskUserQuestion-worthy DESIGN decision between real options.
- Wave facts: `BuildWorkflowItemSchema` has **no `depends_on` and no `wave`** (Peter's own feasibility finding, SESSION-2026-05-23_02:2963); "wave" appears 16 times in `plan/`, 0 in `build/`, all prose; `build/SKILL.md:67` says one-TASK-at-a-time/NO-batching while `implementation-phase-workflow.md:261` says "next TASK in dependency order" (carry the trailing clause — it partly undercuts the forbids-parallelism reading); the hand-authored exemplar is `examples/PLAN-002-task-breakdown.md:92` with its dependency in a table cell at `:75`.
- The derivation rule already exists and is not to be re-invented: two tasks are parallel-safe **iff** neither is in the other's transitive `Blocked by` closure AND their `Files` sets are disjoint (`feedback_session_protocol.md:237`), with the wildcard refinement and N-way extension at `feedback_proactive_parallelism_check.md:11-16`.
- `build.deps` / `build.scaffold` / `build.tasks` are schema-invalid — `PartIdSchema` (`common.ts:11-15`) admits only `build\.SPEC-\d+`. Record it; do not ask. (Same regex hardcodes `protocol-hardening` phase names into the shared library — flag, don't fix.)

## Step 0 — The map, before any edit  [BLOCKING GATE]

Deliver **four tables** — every site, with file:line and actual text, for: (1) wave/sequencing assertions across `plan/`, `build/`, `examples/`, composition; (2) iteration/retry bounds with their numbers; (3) commit-boundary rules during build; (4) visual-QA requirements. Plus **the migration digest** (R-26): the 23 memories Step 7 moves, one line each — what rule each carries into which destination — so Peter sees what is moving before it moves; auto-memory content is assistant-authored and unratified, and migration is destination-first re-authoring, never relocation-as-ratification. Then **wait**. If a table has one row, that is a finding. If the map disagrees with a count above, the map wins — say where.

## The interview — things Peter is wondering about  [WONDERING]

Raise each at its marked moment, once, plain language, one decision in flight. Record answers verbatim; never re-ask.

**W-1 (first — it decides whether derived waves are executable or merely descriptive): what does "NO batching" mean?** `build/SKILL.md:67` says one TASK at a time, no batching; his QA-parallelism memory says dispatch one QA agent per TASK in parallel; his parallelism memory sends 3+ parallel-safe tasks to agent teams. These cannot all be globally true. Options: (a) per-lane — one TASK at a time within a lane, lanes parallel [assistant recommendation: the only reading under which derived waves execute]; (b) global — waves become documentation only; (c) global by default, explicit per-part parallel opt-in. Whatever the answer, the wording changes in **both** files identically (`SKILL.md:67`, `implementation-phase-workflow.md:261`).

**W-2 (before touching cap text): which of the three existing sources of the iteration cap becomes canonical?** The corpus already answers "3" three times — the autonomy memory's halt list, the a–u cycle's step s-fail, and the schema's `max(3)`. Peter is not being asked to invent a number; he is ratifying where it lives. Options: (a) standardise on 3, schema-enforced, everything else references it [recommendation — zero code change, three sources agree]; (b) 5 (schema + every text changes); (c) per-TASK configurable, default 3 (new field, second home for the number). Failure mode if it lives twice: text says 5, schema throws at 4, operator gets a stack trace instead of a halt.

**W-3 (when writing parallel-build-mechanics): the concurrency ceiling.** SKILL-002 C-10 says never pre-negotiate agent budget (30–50 on one topic is normal); his rate-limit memory measured 14 of 17 parallel analysts failing and set "staggered waves of 5–8 **for research-heavy work**" (scope clause is load-bearing). Options: (a) keep C-10 as the depth principle and the measured 5–8 as the operational ceiling for research-heavy dispatch [recommendation]; (b) lower the depth expectation; (c) treat the 2026-05-18 failures as stale and raise the ceiling.

**W-4 (same file): module-only agents + orchestrator-serialised shared files — the ledger's Q15, never yet asked.** PLAN-002's four parallel lanes were made safe by one hand-written line: agents produce module-only output; all shared-file wiring is orchestrator-owned and serialised (`PLAN-002:9, :77, :92`). Options: (a) standing rule in `build`, shared-file set derived from the union of the wave's `Files` sets [recommendation — computable from the same graph as the waves]; (b) standing rule, per-part declared set (cheaper, drift-able); (c) per-plan judgement with a mandatory prompt at wave approval. The answer lands in `parallel-build-mechanics.md` as rule 9, next to the commit-interleaving rule that explains why it works.

**W-5 (when writing visual-qa.md): how deep does Figma wire in, per phase?** Multiselect, one section: (a) `spec` pulls visual requirements from Figma when authoring REQ/DESIGN; (b) `build` hands the implementer the Figma node alongside the DESIGN note; (c) QA diffs the built surface against the Figma frame (already the SS-79 requirement); (d) all three, with the node reference living on the DESIGN note and reached via `implements` [recommendation — one source of truth for the node].

**W-6 (before wiring derivation): the derived-wave failure policy.** What happens when the graph is incomplete or the derivation looks wrong. Options: (a) halt on an incomplete graph (a missing `depends_on` is a spec defect, not something to route around); surface the derived wave plan once per build part for approval; pinned override lives **on the TASK note** so the graph stays the single source of truth [recommendation]; (b) same, override on the PLAN part (drifts); (c) no override — fix the spec (purest, slowest); (d) best-effort + warning + continue — listed to be visibly rejected (P4-12 escape hatch). Failure mode to name in the skill: a missing dep dispatches two agents into one lane; whoever commits first sweeps the other's edits; QA passes both against a tree neither produced.

**W-7 (at Step 6, before wiring any writer): who flips PLAN/TASK statuses and appends SESSION events?** R-20's staging frames it and day-one single-writer is the working default, but Peter explicitly left the fork open — his own words: hooks "feel both like the right thing… and maybe a lot — which is why part of me is leaning towards orchestrator-single-writer — but maybe it's actually not that bad and we should do it?" Options: (a) orchestrator single-writer invoking the shipped mutation scripts [recommendation — R-20's day-one stage; serializes under parallelism; matches his no-autonomous-agent-writes memory]; (b) workers own their bookends; (c) hybrid — workers flip the TASK note only, orchestrator mirrors; (d) hook-performed writes now (event-driven, most reliable in principle; the two caveats: the 2026-06-30 de-registration post-mortem must land first, and hooks lack direct MCP access). Prompt 10's event-authorship column consumes this answer.

**W-8 (with W-7): does the implementer get a symmetric completion gate?** The QA deny gate is decided (item 12). Open: whether an implementer claiming completion is likewise gated on (1) a State block returned and (2) DoD checkboxes touched. Options: yes, same hook pattern [recommendation]; no — QA catches it one step later; defer until the QA gate has run for a while.

**W-9 (when installing templates): the enforcement point for malformed returns.** Until something mechanically consumes the State block, the templates are convention. Options: (a) orchestrator reject-and-redelegate on malformed returns (the exemplar's pattern, re-derived); (b) the R-18 deny gate does double duty (structural template check rides the same hook); (c) staged per R-20 — convention now, gate later [recommendation — matches the decided staging]; (d) hard-require now via a parser (contradicts the no-Zod-for-now ruling; listed to be visibly rejected).

**W-10 (last): wider template adoption.** v0.3 scopes the standard to the three build-loop agents by Peter's own narrowing. Open only whether/when other specialists (analyst, architect, critic, explainer…) adopt: (a) not now — revisit after the build loop has run real work [recommendation]; (b) next prompt that touches each agent adopts it opportunistically; (c) schedule a dedicated adoption pass.

## Execution steps

1. **Derived waves.** Honor the two verify-first preconditions (prompts 5 and 8, above). Migrate the parallel-safety biconditional into `build` (or `plan`, per the Step-5 split) as the stated derivation rule — do not invent a second one. Then compute a wave plan from `depends_on` for one real SPEC and compare it against the hand-authored `PLAN-002:92` waves; report the comparison either way — a mismatch that names what the graph is missing is an equally valid outcome. Where the derived plan lives follows the cheaper of: schema gains `depends_on`/`wave` per item, or derivation lives entirely in `build` at execution time with the PLAN keeping only its rollup. State which and why [DEFAULT: whichever is cheaper after W-1].
2. **One iteration cap**, per W-2: one number, one home, referenced everywhere else.
3. **`build/references/parallel-build-mechanics.md` (new)** from the shared-tree memory's eight findings plus the slice-commit rule — verbatim where load-bearing: the bidirectional commit-interleaving rule (record, never amend); combined close commits for entangled fences ("a broken intermediate commit is worse than a recorded combined one"); the point-in-time window probe; sibling mid-TDD noise (re-run once, report, never fix); **the `git stash` prohibition on shared trees** (the ~2s race that lifts a sibling's uncommitted edits); part-completion push mechanics; the DOUBLE-CLAIM rule ("a claim is consummated by DISPATCH, not by the ledger line"); beyond-band ID allocation. Slice-commit: "the slice is the atomic-commit unit, not the TASK," per-TASK QA and PLAN transitions preserved, deferral recorded, detection signal carried verbatim — the memory argues against its own escape hatch; carry that argument. Add rule 9 per W-4 and the ceiling per W-3.
4. **`build/references/visual-qa.md` (new)** from the three visual memories — carry verbatim where load-bearing: Figma node screenshot into the QA brief as the authoritative target; verify the WHOLE render (computed-style/geometry + a human-legible screenshot; unit tests pass through layout breaks); "if the impl matches a drifted note, the QA 'PASS' is invalid" + same-turn ERRATA; the dark-mode manager-URL rule (`&globals=theme:dark`, never `iframe.html` — false-negative FAIL) with its diagnose-URL-first order; render truth beats stated value, corrections as dated ERRATA. Name the two source defects (the collapsed `<td>` grid that passed QA; the drifted REQ-019 toolbar). Triggering is decided (item 5): via the `implements` chain, no flag — a `visual:` field is a second source of truth that ships the drifted affordance with an extra field to blame. **Verify what resolves before writing**: `chrome-devtools-mcp` is `true` at `settings.json:237` and the figma MCP server is registered (`:331-333`), but the `figma:*` skill family is uncorroborated — check, write the reference against what exists, and record which. The brain-side gap (brain:🧠-qa has zero visual content; `agents/qa.md` never names TASK/REQ/DESIGN as notes to read) is recorded as a handoff to prompt 11 — the brief is rendered skills-side and the skill's brief wins (D-13); brain agents are untouched here except item 13's appends.
5. **Progressive build-section authoring** (item 7): copy `spec` Stage 1's shape exactly — compute a part payload and hand it to `/plan` (`spec-decomposition.md:230-241`); never author PLAN markdown (it would reintroduce hand-transcription and the renderer would destroy it anyway). Emit: ordered wave plan, `build.SPEC-NNN` part definitions, dependency edges. The analysis agent: `brain:🧠-analyst`, mirroring `spec` Stage 1's dispatch [DEFAULT — copy-the-existing-model; if a concrete capability conflict surfaces, raise it as a ranked P4-4 finding rather than quietly substituting].
6. **Install the message standard (item 9) and the writer/gates per W-7..W-9.** Append the dispatch template to `orchestrator.md` and the return templates to `implementer.md` / `qa.md`, exactly as `MESSAGE-STANDARD-DRAFT.md` v0.3 specifies — light appends, nothing else in those files changes (item 13). Reconcile `dispatch-implementer.ts` **and** `dispatch-qa.ts` to render the orchestrator's dispatch template — closing the QA asymmetry so both agents get the pull-model brief (item 2). Wire the R-18 QA deny gate (item 12) at its hook point using `validateQaPassClaim` (P4-13: wire, don't rebuild); the denial event lands in the session note per the W-7 writer answer. Run the brain hook post-mortem (why de-registered 2026-06-30) and report it — it gates R-20 stage 4, not this prompt's work. All tooling pure Bun (item 14).
7. **Migrate the 23 memories** (20 build + 3 `review` riders) per the Step-0 digest, destination-first re-authored [R-26]: eleven into `build/SKILL.md`'s body as early standing instruction (the autonomy halts; the a–u pointer — prompt 2 already collapsed it, point at it; the strict-halt trio; the checkbox sweep; the unblock cascade; the five wave-graph drift modes; the enum crosscheck pair merged into one; QA-parallelism per W-1's answer); two into `parallel-build-mechanics.md`; three into `visual-qa.md`; two into a new `dispatch-briefs.md` (no-autonomous-git-or-plan-writes; complete-rename-no-backcompat); one into a new `implementer-dod.md` (the five-package-manager lockfile table — the table at `:31-35` and the dispatch template at `:39-52` are different ranges, carry both); one into a new `build-hazards.md` (hooks go live the instant a handler file exists); three `review`-body riders (fix-scope calibration; triage-before-posting; skip self-authored PRs). Today `ls build/references/` returns exactly `exit-gates.md`, `implementation-phase-workflow.md`, `state-propagation.md` [FACT] — the five new files are created as new files; if an unmarked path is absent, stop rather than create silently. **Four conflicts are reconciled on migration, never absorbed:** (C-4) strict-halt vs continue-with-independent-work — pick one; the halt memory is unambiguous and P4-12 says fail closed [DEFAULT: strict halt]; (C-8) one-at-a-time vs K-parallel-QA — this is W-1; migrate the winner, rewrite the loser, and drop the paragraph the QA memory itself marks OBSOLETE while keeping its surviving pre-allocated-QA-NNN requirement; (C-9) three commit-boundary rules — compatible as one default plus two named exceptions with detection signals, stated that way; (A1 E-1) the "canonical enum" fiction — five real build statuses are missing from the enum the crosscheck memory validates against; publish **per-note-type enums from the schemas** (`PartSubstatusEnum`, `TaskStatusEnum`, `PlanStatusEnum`, `SessionStatusEnum` in `common.ts`; `BuildWorkflowStatusEnum` in `plan-note.ts:31`) and have the crosscheck select by note type — completing a distinction the memory itself started at `:23-25`. **Gate 3** (`exit-gates.md:37-71`): the orphan-ref INFO-and-continue branch is an open escape hatch in a skill that forbids them — either the validator exists after this prompt or Peter explicitly accepts the branch with the acceptance written next to `:39`; bring the choice with the findings, not as a W-item. **EH-23** generalised: an unresolvable enum/status/classification routes to `AskUserQuestion`, never to a default or a documented-rationale continue — stated once in `build`'s body. **EH-28**: the Jira-sync skip survives only with its record-the-manual-step-as-an-Event condition.

## Boundary

- `end/SKILL.md:49-50`, `:418`, `end-checklist.md:250-262` — prompt 10's; untouched. The `/end` twelve-frontmatter-key exposure is prompt 10's; note in the handoff.
- `brain/agents/**` untouched **except** the three template appends (item 13); `brain/rules/**` untouched (prompt 11).
- No auto-memory is deleted (prompt 12 owns the layer); the 23 migrated sources stay on disk.
- No PLAN-side validation is added here (prompt 5's lane).

## Independent evaluation

Per P4-4/D-2 on `build` (and `review` if touched): cold-read, ranked findings, nothing applied without approval. Read before designing — at minimum: `build/SKILL.md` + all of `build/references/`, both dispatch scripts + tests, `schemas/plan-note.ts` + `common.ts`, `renderers/plan-note.ts`, `spec-decomposition.md`, `auto-routing.md`, and the 23 memories.

## Git

One branch; coherent commits (waves / cap / references / template installs / migrations are separable); unmerged; no push; no `--no-verify`; no AI attribution.

## Done means

- [ ] The Step-0 map (four tables + the 23-memory migration digest) was delivered and answered **before any edit landed**.
- [ ] **Every W item was raised at its marked moment, once, in plain language, and its answer is recorded verbatim in the report. No W item was decided silently, and none was asked twice.**
- [ ] Prompts 2, 5 and 8's prerequisite landings were verified (or the run stopped and said which is missing).
- [ ] Wave derivation: the schema-vs-execution-time choice is stated with its reason; the real-SPEC comparison against `PLAN-002:92` is reported either way.
- [ ] "NO batching" has one unambiguous scope, stated identically in both files that define it.
- [ ] One iteration cap, one number, one home, everything else referencing it.
- [ ] `parallel-build-mechanics.md` carries all eight shared-tree findings + the slice rule verbatim where load-bearing (git-stash prohibition, DISPATCH-consummates-claims, slice-is-the-unit), plus rule 9 per W-4 and the ceiling per W-3.
- [ ] `visual-qa.md` carries the three memories verbatim where load-bearing (invalid-PASS rule, iframe false-negative rule, dated-ERRATA rule); the figma-skill-family resolution was checked and reported; triggering is via `implements`, and no `visual` flag exists anywhere.
- [ ] `build` computes a part payload and hands it to `/plan`, mirroring `spec-decomposition.md:230-241`; the analysis agent is named with its citation.
- [ ] The three agent files end with their v0.3 templates, appended and nothing else changed; both dispatch scripts render the dispatch template (asymmetry closed); the QA deny gate is wired per item 12 with `validateQaPassClaim`; the brain hook post-mortem is reported.
- [ ] All 23 memories live at their destinations and read back; sources still on disk; the four conflicts reconciled as specified; per-note-type enums published; Gate 3 resolved or explicitly accepted in writing; EH-23 stated once; EH-28 conditioned.
- [ ] `quick-validate.ts` passes for every SKILL.md changed; description benchmark shows no regression against the prompt-1 baseline.
- [ ] Ranked findings delivered; nothing applied without approval.
- [ ] Boundary respected: `end/` untouched at the three sites; brain agents untouched beyond the appends; nothing deleted.
- [ ] One branch, coherent commits, unmerged, no push, no AI attribution.
