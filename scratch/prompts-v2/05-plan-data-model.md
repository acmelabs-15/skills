# Prompt 5 — PLAN data model: one definition, one status vocabulary, a parser that reads real PLANs

_Run after prompt 1 (`freeze-and-baseline`) and prompt 2 (`cycle-collapse-and-brief-contract`). Prompts 7 and 9 depend on this one; prompts 7, 8 and 10 subset the status atoms defined here (R-2); prompt 10 consumes the session-schema keys and the event-schema home (R-4/R-6); prompt 11 consumes the inter-plan relations and owns their routing (R-10). Paste everything below into a fresh Claude Code conversation._

Repository root: `/Users/peter.kloss/Dev/ACMElabs/skills`. Paths are relative to it unless they start with `/`. Read-only evidence corpus: `/tmp/corpus/`; Peter's seven real PLAN notes are copied to `/tmp/corpus/examples/` and live in their projects' `docs/planning/` directories.

Human attention in this prompt: the Step-0 map gate, the W-items below at their marked moments, two presentation approvals (the STAYS/MOVES/GOES audit; the independent-evaluation findings), plus whatever gates `skill-creator`'s own lifecycle raises. Everything else is decided, measured, or a reported default — and the file:line facts below are a working set the Step-0 map confirms or corrects, not things to re-derive mid-task.

---

**Execution contract (R-31): before doing anything else, read `scratch/prompts-v2/RUN-CONTRACT.md` — it sits beside this file — and run this entire prompt in its collaborative mode: granular, guided, opinions labeled, no assumptions. It overrides any "autonomous" framing below.**

## How to read this prompt — the provenance register

Every substantive statement below carries one of four tags. This is the contract that keeps Peter's decisions his, and the assistant's analysis labeled as analysis:

- **[DECIDED]** — Peter decided it, dated, in his own words where quotable. Do not re-litigate and do not re-ask.
- **[WONDERING]** — Peter's open questions. These ARE the interview: raise each at its marked moment via `AskUserQuestion`, exactly once, with the full relevant text shown. Attached recommendations are the assistant's, never his. Nothing in this register is pre-decided, however confident the recommendation sounds.
- **[FACT]** — measured repo or tool reality, with reproduction where it matters. Verify before relying on one; report mismatches as drift, not as license to improvise.
- **[DEFAULT]** — assistant design suggestions. Adopt or improve at runtime; deviations are *reported* with the measurement that justified them, not asked about.

If anything below reads as [DECIDED] but smells like an assumption, stop and surface it. That is a defect in this prompt, not a constraint on you.

## The authority rule  [DECIDED 2026-07-27]

**`skill-creator` (the `skill-creator@ACMElabs` plugin, `/Users/peter.kloss/Dev/ACMElabs/skill-creator/skills/skill-creator/`) is the authority for skill-authoring mechanics** on every SKILL.md this prompt touches (`plan`, `research`, `decisions`). Load it, let it drive its own lifecycle, evals and validators at its own pace, and do not restate its internals — this prompt supplies only repo facts, Peter's decisions, and Peter's open questions. Where anything here appears to conflict with `skill-creator` about *how* to author or test a skill, `skill-creator` wins; say so in the report. `grill-me` (user-level) is available for interview segments; plain `AskUserQuestion` per P4-1 is otherwise fine. [DEFAULT]

**The predecessor `create-skill` was deleted 2026-07-28 (R-32)** — substitution, not rename. Four deltas bind on any skill edit: descriptions are **capability-first with a deliverable clause and a same-domain exclusion, and pushiness is a measured defect** (the old "be pushy" advice is inverted); body size is a **dual limit — under 500 lines AND under 5,000 tokens**, both reported; directory placement is decided by the verb SKILL.md uses, not file type; and the validator is `bun scripts/quick-validate.ts <dir> [--standard|--extended]`, not the retired Python one. `skill-reviewer` now ships in-plugin as `skill-creator:skill-reviewer`, so it resolves. `skill-creator@claude-plugins-official` (`settings.json:245`) and `plugin-eval@claude-code-workflows` (`:241`) remain `false`; `skill-creator@ACMElabs` is `true` (`:269`) and is the live path. [FACT, verified 2026-07-28]

**Evidence authority: A4 > the maps > the conversation ledger's findings.** The ledger's PART-1 decisions are binding; several of its *findings* were refuted on inspection and the corrected text is already substituted below. Three corrections that matter here [FACT]:

- The PLAN adapter does **not** safely split a PLAN and structurally cannot (adapter facts below). Do not ask Peter to confirm preservation that can never pass.
- `--split` is **seven halt files carrying 18 split-naming lines** plus binding sites in the `plan` skill itself, including its frontmatter `description` — not the ledger's "five sites".
- The PLAN note is **not** the dispatch-brief carrier; prompt 2 already deleted the five prose sites that said so [DECIDED — R-15/R-16, executed in prompt 2]. `BuildWorkflowItemSchema` (`shared/composition/src/schemas/plan-note.ts:41-52`) has no free-text field at all; `build_workflow_items` is a derived per-TASK progress rollup, which is its real and working function, and it stays [DECIDED — R-11: tasks surface in the plan as derived state only].

## Standing rules (programme-wide)

- **P4-1 — Every question goes through `AskUserQuestion`.** Max **4 authored options**; "Other" is auto-appended, so never author one, and say in the question text that Peter can type his own answer there. Questions are written in **plain language** — no internal jargon, no stage numbers, no file:line in the question body unless Peter is ruling on that exact text; what breaks and what it costs, stated simply. (A fuller ASK-STANDARD is being developed in prompt 7's interview [DECIDED — R-29]; until it lands, this paragraph is the standard.)
- **P4-2 — One decision per question, one question in flight.** A multiselect decomposing a single decision is not a bundle.
- **P4-3 — Author the moment it locks.** Never defer; apply each answer before asking the next.
- **P4-4 — Independent evaluation mandate on every skill touched (D-2).** Evaluate as if handed the skill cold. Ranked findings, each with a recommended action and one-line rationale. Apply nothing without approval. Honest short list beats padding.
- **P4-5 — The full `skill-creator` lifecycle runs for every skill touched, under the skill's own procedure** (authority rule above). The one repo fact the skill cannot know: **prompt 1 already took the week-0 snapshot** off-tree — find it, point evals at it, never re-baseline. If it does not exist, stop and tell Peter.
- **P4-6 — Binary tool rule.** `docs/**` → Brain MCP; everything else (skill bodies, references, config, `.ts`) → `Read`/`Edit`/`Write`, on which Brain MCP is forbidden. Route by artifact, not by phase: most of this prompt is `.ts` and skill markdown, but the real PLAN notes in `docs/planning/` trees (this repo's own included) are graph territory, and `/tmp/corpus/` is read-only evidence. Normative source: `~/KNOWLEDGE-GRAPH-CONVENTIONS.md:184-186`, `:202`.
- **P4-7 — Git.** Verify the branch first. One branch for the whole prompt. Coherent commits (≤5 files or one logical change). Leave unmerged. Do not push. No `--no-verify`, no force-push. No indication of AI contribution in any commit message.
- **P4-8 — Update cost is work, not an argument (D-19).** *"I wanna do something because it's the right decision to make."*
- **P4-9 — Nothing deleted in bulk; nothing deleted before its destination is live and verified** (read-back or exercise, not assertion). `Bash(rm:*)` is in the permissions `ask` list (`settings.json:158`): every deletion raises a prompt even when approved in principle. Expect it; never route around it — no `mv`-to-scratch, no `git rm`, no redirect, no truncate, no removal script. A blocked deletion is recorded as deferred, not worked around.
- **P4-10 — Read before designing, and say what was read. Do not sample.**
- **P4-11 — Precedence:** skills plugin > brain plugin > home specs > auto-memories. Where a home spec or an auto-memory disagrees with the `plan` skill about what a PLAN note is, the skill wins (D-13).
- **P4-12 — Fail closed; never "document a rationale and proceed."** No defensible count of this class exists — the nearest real figure counts *gates*, not rationale branches: `docs/analysis/ANALYSIS-005-skills-ecosystem-enforcement-wiring-deep-analysis.md:64`, *"23 of 27 enumerated gates fully phantom, 2 partial (LLM-advisory), 2 genuinely live."* State a count only with the command that derived it. Models to imitate: `skills/build/references/exit-gates.md:3` (*"'I'll fix in review' is NOT acceptable rationale"*) and `brain/agents/orchestrator.md:1890-1892` (validator cannot run → *"DO NOT claim completion"*). Reject, don't edit down, any proposal that introduces a new such branch.
- **P4-13 — Do not build what already ships; grep for the name before writing any function.** In play here: `validateIntegrityFloor` (`shared/composition/src/core/validate.ts:32`) — fully built, **zero non-test, non-hook callers** (`ANALYSIS-005:61-62`'s wording; bare "zero callers" is false and checkable). Wire, don't rebuild.
- **P4-14 — This is the prompt that makes enforcement claims true. Until a claim is demonstrated by running, write what runs, where, against what.** Populations [FACT]: `parsePlanNote` fails **7 of 7** of the example-collection PLANs and **1 of 2** of this repo's own `docs/planning`; the composition suite is **458 synthetic-fixture tests** (`docs/qa/QA-030-…:35`, `:38` — the figure "82" is sourced nowhere; do not repeat it). The load-bearing exception: `shared/composition/tests/plan-001-migration.test.ts` runs `parsePlanNote` plus a SHA-256 round-trip against the repo's real 99,276-byte PLAN, and passes. The honest form: enforcement has never executed against Peter's seven PLANs, and the one real PLAN it does execute against is in the opposite shape. Do not assert enforcement you have not run, and do not assert non-existence you have not grepped for.

---

## Facts — three PLAN definitions, and a fourth with a shipped test behind it  [FACT]

- **Definition A — `skills/plan/references/plan-note-schema.md`.** Self-declares authoritative (`:3`). Thirteen ordered top-level sections (`:93-105`), parts as H3s under **phase H2s**, bold-paragraph per-part fields, six per-part H4 subsections (`:124-140`), `d_n_substatus` carrying `{id, status, decision: <verbatim text>}` (`:56-57`). No code reads it.
- **Definition B — the composition library** (`schemas/plan-note.ts`, `parsers/plan-note.ts`, `renderers/plan-note.ts`). Parts are H3s under `## Phase Progression` (`renderers/plan-note.ts:205-214`), fields are bullets, the schema is `.strict()` over eleven keys. All the enforcement power, no claimed authority.
- **Definition C — Peter's seven real PLANs.** Parts live under phase H2s; `## Phase Progression` is a four-column summary **table**, not a container (`PLAN-004…:71-79`; its five part H3s sit under `## Build`, `:250`–`:487`). Per-part fields are bold paragraphs (`PLAN-004:252-256`). **A and C agree; B is the odd one out, and B is the one with the power.**
- Two Definition-C details B does not model, which must survive whatever W-1 decides: **six of eight `BuildWorkflowItem` bullet names differ** (renderer `renderers/plan-note.ts:194-201` vs `PLAN-004:262-269`: five differ by case, one by name — `Test report ref` vs `QA Ref` — and values differ, `Event 34` vs `34`). `parseBuildWorkflowItems` looks up `fm.get("Task Ref")` (`parsers/plan-note.ts:187`) and bails at `:190`, so **every build workflow item in every one of the seven is silently discarded** — the guard is currently hiding the field-name defect. And `PLAN-004:246` records a live constraint the schema ignores: the basic-memory parser rejects a `[[wikilink]]` inside a `**bold**:`-prefixed bullet, so task refs are written as plain-text identifiers.
- **The fourth definition — `skills/docs/planning/PLAN-001-skills-ecosystem.md`** (99,276 bytes, this repo's own PLAN): authored in B's shape. `## Phase Progression` at `:133` is a *container* holding 25 H3 parts; `## Tasks` (`:3343`, 27 populated `T-NN` rows under `### Archive`), `## Pending User Decisions` (`:3385`), `## Editor Mirror IDs` (`:3389`); no `## Workflow Plan`, `## Decision Log`, `## Progress Log`, `## Risks`. `shared/composition/tests/plan-001-migration.test.ts` asserts exactly that shape — the four `.not.toMatch` absences, the three `.toMatch` presences, `parsePlanNote` passing, SHA-256 round-trip identity — citing REQ-012-SPEC-007 and ADR-003 D-6. It parses and re-renders **byte-identical** (25 parts, 27 tasks; verified by running). **It is the only definition simultaneously specified, implemented, produced and tested.**

## Facts — the destruction sequence, or why the section model is fixed before the parser  [FACT]

- `parsePlanNote` (`parsers/plan-note.ts:349-374`) builds its model from exactly **nine named H2s** — Scope, Objectives, Phase Progression, Tasks, Pending User Decisions, Editor Mirror IDs, Blockers, Observations, Relations — and **discards every other H2 before validation**, so `.strict()` never fires on them. `renderPlanNote` rebuilds the whole document from that model alone, and `skills/plan/scripts/render-plan-note.ts:115` writes it back: `await Bun.write(args.planPath, after);`.
- Destroyed on a **successful** render, all actively authored:

| Section | Population in the seven | Authored by |
|---|---|---|
| `## Risks` | 5 of 7 exact; `PLAN-004:125` is `## Risks (pre-mortem)` — match prefix-tolerant | `plan/SKILL.md:169` (create Step 8 pre-mortem), `build/SKILL.md:212` |
| `## Workflow Plan` / `## Decision Log` / `## Progress Log` | 6 of 7 each (same six) | `plan-note-schema.md:96`, `:99`, `:100` |
| **Every part-bearing H2** (`## Research`, `## Build`, …) | 6 of 7 — **where all the parts live**. Two use non-bare names: `PLAN-001-aggrid:135` `## Research Parts (kickoff Phase 0–4)`, `:292` `## Spec Parts (kickoff Phase 5–6)`; `PLAN-002-datatable:148` `## Build Parts`. Never match an exact phase-name list | `orchestrator-routing-protocol.md:112`, `workflow-migration.md:180` |
| `## First-Principles`, `## Open Parallel Threads`, `## Deferred — Next-Plan Hand-off`, `## Effort Tracking` | ad hoc: `PLAN-004:43`, `:153`; `PLAN-003:131`; `PLAN-001-fond:548` | hand |

- The renderer's docstring (`renderers/plan-note.ts:22-24`) claims it regenerates two derived sections. It regenerates the entire document; anything not in the model is gone.
- **Today the system fails safe**: parsing fails on all seven, so the write at `:115` is never reached. **Fixing the parser first converts a silent no-op into silent destruction of `## Risks` and the phase headings** — no error, no diff shown, no rollback. It also re-arms a gate: `build/SKILL.md:172` skips Steps 1–3 only while `## Risks` is populated, so one render between two `/build` invocations silently re-runs a pre-mortem Peter already did.
- **Operational rule for the whole session: `render-plan-note.ts` is not run against any real PLAN until the section-model change is committed.** Synthetic fixtures until then, and say so when you use them.
- The destruction is specific, not general: it happens to documents authored in the A+C shape — all seven of Peter's, and not the repo's own `PLAN-001-skills-ecosystem.md`, which is already in the renderer's shape. Say it that way.

## Facts — the parser census: six root causes  [FACT]

`parsePlanNote` fails 7 of 7 in `/tmp/corpus/examples/` — executed, not inferred; this is the actual first-error output:

| PLAN | First failure |
|---|---|
| PLAN-001-aggrid-tanstack-bridge | `permalink` regex; `parts` too_small |
| PLAN-001-fond-monorepo-pwa | `parts` too_small |
| PLAN-002-datatable-feature-implementation | `Parse error at <root>: expected string, got undefined` |
| PLAN-002-task-breakdown | `Parse error at <root>: expected string, got undefined` |
| PLAN-003-server-side-row-model | `permalink` regex; `parts` too_small |
| PLAN-004-datatable-client-side | `permalink` regex; `parts` too_small |
| PLAN-006-polar-ui-mcp-extraction | `parts` too_small |

Zod reports the first failure it reaches; three causes are latent behind the visible ones and surface one at a time as you fix upward. **At least six independent root causes; fixing any five leaves PLANs still failing. Budget for it.**

1. **`parts: []` on all seven.** Parts are read only from `Phase Progression` (`parsers/plan-note.ts:362`); `schemas/plan-note.ts:260` requires `parts: z.array(PartSchema).min(1)`. The seven keep a summary table there and put parts under phase H2s.
2. **The `permalink` regex fails 5 of 7.** `schemas/plan-note.ts:93` is `/^planning\//`; Brain MCP emits project-scoped permalinks (`datatable-next/planning/plan-004-…`). Only the two PLANs outside `datatable-next` pass.
3. **Both PLAN-002 files lack required frontmatter** and declare `type: note`, so `asString(raw["status"])` (`parsers/plan-note.ts:48`) hits the throw at `:35`. [DEFAULT] Repair the notes (Brain MCP — they are `docs/**`) rather than widening the schema to admit `type: note`; report which you did and why.
4. **`PartIdSchema` rejects 16 of 51 real part ids** (`common.ts:11-15`) — latent until Cause 1 is fixed. One rejected part fails the whole document, so the operative figure is **five of six part-bearing PLANs die**; only `PLAN-001-fond` (0/17 rejected) survives. Rejections: `PLAN-001-aggrid` 7/7 (`phase-0`…`phase-6`); `PLAN-002-datatable` 3/9 (`build.deps`, `stories`, `intl`); `PLAN-003` 2/5 (`spec`, `build`); `PLAN-004` 3/7 (`decisions`, `spec`, `build`); `PLAN-006` 1/6 (`build.SPEC-NNN`, which `PLAN-006:216` labels *"placeholder (defined at spec-decomposition)"*). Three classes — bare phase names, project-local vocabulary, placeholders — and W-5 rules on a fourth. Bring the classes, not just the count; use 16 of 51, not a rounder number.
5. **`PLAN-002-task-breakdown.md` is not a PLAN under any definition.** No `## Scope` / `## Objectives` / `## Observations`, zero H3 parts, one relation (`part_of [[PLAN-002: …]]`), `type: note`, and its title fails `/^PLAN-\d+:/` (`schemas/plan-note.ts:88`) — five independent schema failures on top of Cause 3. Its disposition is **W-2**. Never widen the PLAN schema to admit it, and never count it against the success claim.
6. **The `BuildWorkflowItem` field-name mismatch** (facts above) — fixed in this pass, not deferred: case-insensitive lookup plus an explicit `Test report ref` ↔ `QA Ref` alias, or whatever W-1's answer implies. [DEFAULT]

Same pass, because each is a silent wrong answer rather than an error: `optNum` (`:155-161`) parses `Event 34` to nothing instead of 34; DoD is read positionally as "the second list in the part body" (`:126-130`); the first table in a part is taken as the Decisions table (`findTable`, `:133`); and `parseBuildWorkflowItems` silently skips malformed items at `:190` — a straight P4-12 violation; make it fail loudly.

**Definition of done for the parser: `parsePlanNote` succeeds on all six real PLANs in `/tmp/corpus/examples/` that are PLANs, plus a recorded W-2 ruling — demonstrated by running it and pasting the output, not asserted.** (If W-2 rules it stays a PLAN, it is seven of seven.) Then re-run the Step-1 round-trip against all of them (empty diffs), the full composition suite, and `plan-001-migration.test.ts` specifically — a green parser with a red migration test is W-1's collision arriving late, not done.

## Facts — split, scope, migrate  [FACT]

**`--split` has never been executable.** Three independent proofs: (1) `PartIdSchema` (`common.ts:11-15`) admits no `.split`/`.a`/`.b` while `plan-note-schema.md:85` specifies exactly those; (2) `PartSubstatusEnum` (`common.ts:34-42`) has no `SPLIT` while `plan-note-schema.md:114` and `scope-evaluation-and-split.md:200` (*"`SPLIT` (terminal)"*) require it — and `TERMINAL_PART_SUBSTATUSES` (`schemas/plan-note.ts:247`) is `["DONE","DEFERRED","ABANDONED"]`, so a SPLIT part would permanently block PLAN DONE; (3) no `DRAFT` either, which `scope-evaluation-and-split.md:94` and `workflow-migration.md:155`, `:184`, `:228` require.

**Seven halt files, 18 split-naming lines** (the ledger's "five sites" was wrong; three further lines say `--scope` and belong to that mode): `research/references/resource-bounds.md:26,32,44,180`; `research/references/convergence-protocol.md:142`; `research/references/analysis-phase-workflow.md:205,228`; `decisions/references/pre-author-composite-gate.md:3,63,82,100,117`; `research/SKILL.md:129,151`; `decisions/SKILL.md:145,217`; `decisions/references/decisions-phase-workflow.md:147,259`. Sample: `resource-bounds.md:44` — *"1. Split via /plan --split (Recommended)"*.

**Four invocation spellings**: `--split` (with and without `PLAN-NNN`); `/plan split` (`research/SKILL.md:151`); `Skill(skill="plan", args="split")` (`pre-author-composite-gate.md:63`, `decisions-phase-workflow.md:147`, `decisions/SKILL.md:145`, `:217`); `args="split PLAN-NNN decisions.{N}"` (`pre-author-composite-gate.md:100`). The single gate that catches all four: `grep -rniE -- '(--split|/plan split|args="split)' skills/` → 0 outside history. A single-spelling grep is **not** the gate — `grep -rn -- "--split"` returns 0 while five sites survive.

**Mode binding sites** (derive the list yourself; this working set has had one hole already): `plan/SKILL.md:3` — the **frontmatter `description`**, which names the trigger phrase *"split PLAN-NNN"* and the mode list and is what routes invocations — plus `:18`, `:91`, `:94`, `:108`, `:146`, `:186-188`, `:190-192`, `:284`; `plan-note-schema.md:85`, `:114`; `workflow-migration.md:116,134,140,150,152,155,184,210,228,242`. `two-step-edit-pattern.md:64` is **not** a site (`grep -in split` there → 0; do not edit it). `scope-evaluation-and-split.md` (260 lines) is deleted only once nothing points at it — destination first (P4-9), `rm` prompt expected.

**`--scope` is `--split`'s read-only half.** `plan/SKILL.md:190-192` is a heading plus one prose line; it names no reference file (the pointer to `scope-evaluation-and-split.md` lives in the Split section at `:188`, so deleting Split strands Scope's only implementation), no pipeline, no halt IDs of its own — but **three live halt-site references of its own**: `convergence-protocol.md:95`, `analysis-phase-workflow.md:185`, `:227`. Whatever W-4 decides, those three get the same replace-before-remove treatment.

**`migrate` is a three-file contradiction, one file asserting both positions.** Never-auto-detect on `/plan create`: `plan/SKILL.md:158`; `migrate-auto-detection.md:3` — which quotes Peter's own clarification, *"no auto-migration in any form"* — and `:9`. Mandatory-sweep: `migrate-auto-detection.md:106` (the anti-patterns row — open it and confirm the cell wording before editing), `:111`, `:30-37`; `workflow-migration.md:5`, `:17`. The 2026-05-19 revision edited the prose header and left the body and table untouched. Separately false either way: *"Migration is idempotent — re-running detects new artifacts and offers to extend existing PLAN"* (`workflow-migration.md:246`) — Step 8.1 (`:176`) always creates a new PLAN and `migrate-auto-detection.md:30-33` halts when one exists.

## Facts — the adapter and the unwired floor  [FACT]

- `shared/composition/src/adapters/plan.ts` (343 lines): **zero occurrences** of `depends_on`, `owning_session`, or `substatus` anywhere (grepped; count 0). Extraction is a line slice (`:109-113`); mutation is regex find/replace (`:241-244`, `applySinglePassReplace` over `renumber_map` and `wikilink_map`). Part IDs are the **designated mutation surface**, not a preserved one — phase is derived from the part id, so an injective renumber that changes a phase prefix silently reclassifies the part, and no byte proof can see it. Regenerated spans pass through **unmutated** (`:231`), so after a renumbering split the Cross-Part Dependency Graph still names the old part IDs. `reverseMutations` (`:150-154`) calls `enforceIntegrityFloor` — an order-insensitive ≥50% line-overlap count, not a hash.
- Decisively: `decompose.ts:25-49` and `recompose.ts:18-30` import only `schemas/base.js` and `schemas/plan-yaml.js` — **no note schema ever runs in the decompose path**, so `PlanNoteSchema`'s referential checks (`schemas/plan-note.ts:279-288`, dangling `depends_on`) never fire, and no cycle detection exists anywhere in the library.
- The honest statement: **`plan.ts` can slice a PLAN without losing bytes, and cannot tell you whether what came out is still a PLAN.** Schema-aware PLAN decomposition is net-new work.
- `validateIntegrityFloor` (`core/validate.ts:32`): advertised live at `shared/composition/schemas/base.ts:105` (note the path — there is **no** `src/schemas/base.ts`) — *"runtime validates <=50% of source lines"* — with exactly **three invocations corpus-wide**, all in `tests/plan-integrity-floor.test.ts` (`:236`, `:256`, `:273`). Today a distribution plan declaring 10 regenerated sections covering 90% of a note passes every gate, after which the SHA-256 round-trip trivially succeeds because almost nothing was mutated: a gate that certifies its own bypass.

## Facts — the smaller defects this prompt owns  [FACT]

- **Contract 1 exits 2 from every documented call.** The four doc sites (`plan/SKILL.md:221`; `plan/references/auto-routing.md:54`; `build/SKILL.md:262`; `build/references/implementation-phase-workflow.md:305`) pass `plan=PLAN-NNN` — an identifier — and omit two of the five flags `set-part-done.ts:77-83` requires (`--plan-path`, `--part-id`, `--outcome`, `--owning-session`, `--at-event`). Also: `--at-event` is required, validated (`:93-96`), stored (`:110`) — **and never read**; the mutation payload (`:150-157`) omits it, so the Event linkage the two-step edit pattern exists to guarantee is not recorded by the script performing the state change. `--owning-session` populates `completing_session` (`:155`) — the name contradicts the function. Rationale for DEFERRED/ABANDONED is string-smuggled into `outcome` by `composeOutcome` (`:129-132`) — a finding, not a fix here.
- **`d_n_substatus` cannot hold the verbatim decision — hard prerequisite for prompt 7.** Spec: `{id, status, decision: <verbatim text>}` (`plan-note-schema.md:56-57`). Implementation: `DecisionStateSchema` (`schemas/plan-note.ts:114-120`) has `topic`, no verbatim field; `lock-decision-mutation.ts:107` passes a short label. The `decisions` detail-parity audit compares against a field the schema cannot hold — it is comparing against nothing. Status enums also differ (spec `PENDING|LOCKED` vs schema `PENDING|LOCKED|REJECTED|DEFERRED`).
- **Sections the renderer emits that Peter never authors**: `## Tasks` (`renderers/plan-note.ts:237-259`; its `backlog` partition is hardcoded empty at `:241` — `filter((_t) => false)` — so all 27 real tasks land in `Archive` regardless of status), `## Pending User Decisions` (`:261-285`; `PUD-N` appears in no skill step), `## Editor Mirror IDs` (`:287-301`; written by nothing findable). Absent from all seven of Peter's PLANs — **but populated (the 27 live `T-NN` rows) or present-empty in the repo's own PLAN, asserted by `plan-001-migration.test.ts` AC#1b, and backed by ADR-003 D-6.** Not free deletions.
- **Six per-part H4 scaffolds** (`plan-note-schema.md:124-140`) are emitted and read by nothing — and `parseBuildWorkflowItems` treats **every H4 in a part body** as a workflow item, so any scaffold in a real PLAN would misparse as a malformed item. They survive only because of the `:190` silent skip this prompt removes; delete the scaffolds in the same change that makes the guard loud, or the documented format hard-fails.
- **Hand-ticked derived state**: `build/references/state-propagation.md:54` (inside sub-step 6.3, `:48`) instructs hand-updating `## Progress Dashboard`; `:108` enshrines it as an anti-pattern row. The dashboard is derived-only — parser skips, renderer regenerates; both instructions go.
- **Dashboard collapse**: `renderProgressDashboard` (`renderers/plan-note.ts:76-79`) folds `READY`, `DEFERRED`, `ABANDONED` and `PENDING` into one PENDING column — a seven-value enum in four columns, terminal states indistinguishable from not-started. Report it in the findings with a recommendation; the R-2 atom work is the natural moment.
- Independent-evaluation seeds (fold in or reject): the `/plan create` step list has no Step 2 (`plan/SKILL.md:158-172`); `orchestrator-routing-protocol.md:106` cites Step 4 for authoring that is Step 6; `plan-note-schema.md:27` declares `PAUSED` invalid for PLANs while `PlanStatusEnum` (`common.ts:51`) includes it — the R-2 subset declaration resolves this one way or the other, report which; `skills/plan/.DS_Store` is committed.
- `PhaseEnum` (`common.ts:55`) carries seven phases against the five in `documentation/workflows.html:231` (note: `documentation/`, not `docs/`). Record the mismatch; prompt 11 owns that surface.
- Prompt 1 compiled a register of 48 fictions corpus-wide; **nine are this prompt's** (execution step 15).

## Read before designing anything  [P4-10]

| Path | Why |
|---|---|
| `skills/plan/SKILL.md` + all 8 files in `skills/plan/references/` | The skill, its mode table, Definition A |
| `shared/composition/src/schemas/plan-note.ts`, `schemas/common.ts` | Definition B's schema and enums; R-2's landing site |
| `shared/composition/src/parsers/plan-note.ts`, `renderers/plan-note.ts` | The failing parser; the destroying renderer |
| `tests/plan-001-migration.test.ts`, `tests/plan-round-trip.test.ts`, `skills/plan/scripts/render-plan-note.test.ts` | The three tests this prompt collides with — **before** W-1, not after |
| `skills/docs/planning/PLAN-001-skills-ecosystem.md` (structure only — ~99 KB: the H2 list, `## Phase Progression`, `## Tasks`) | The repo's own PLAN, in B's shape |
| `skills/plan/scripts/render-plan-note.ts`, `set-part-done.ts` | The two CLI entry points |
| `shared/composition/src/adapters/plan.ts`, `core/validate.ts`, `core/adapter.ts` | The adapter and the unwired floor |
| The composition session schema + `adapters/session.ts` | R-4's keys and R-6's schema home land there |
| All 7 files in `/tmp/corpus/examples/PLAN-*.md` | Definition C |
| `skills/decisions/references/pre-author-composite-gate.md`; `skills/research/references/{resource-bounds,convergence-protocol,analysis-phase-workflow}.md` | The halt sites |

## Step 0 — The map gate  [BLOCKING GATE]

Before any edit lands, deliver one table per contested concept and stop: the **PLAN section model**, the **part-id space**, the **`--split` surface** (all four spellings), and the **status-enum sites** R-2 consolidates — every defining site with file, line and actual text. The file:line facts above are the working set, not a verified census; confirm or correct them here. Wait for Peter's go. No reply is a stop, not an implied yes.

---

## The interview — things Peter is wondering about  [WONDERING]

These are Peter's open questions, carried here so they are asked **once, at the natural moment, by you** — not pre-decided in this prompt and not re-interviewed anywhere else. One decision in flight (P4-2). Plain language (P4-1). Show the full current text whenever he is ruling on existing text. Record every answer verbatim in the report; never re-ask an answered item.

**W-1 — at Step 0's close, before any file edit: which PLAN shape wins.** The facts to put in front of him: the four definitions above, and the collision — his seven plans are written one way, the library enforces another, and the repo's own plan was **already migrated to the library's shape by a prior accepted ruling and locked with a shipped test**, so choosing his shape means knowingly reversing that ruling (`plan-001-migration.test.ts` breaks by construction; REQ-012-SPEC-007 needs an explicit disposition), while choosing the library's shape means migrating his seven notes and touching no code or test. Before asking, find and read REQ-012-SPEC-007, ADR-003 D-6 and the migration TASK, and put what they say in front of him — if there was a reason `## Workflow Plan` was removed, he wants it in view before he chooses. Options: (a) the library moves to his shape — **assistant's recommendation, explicitly weakened by the test collision**; (b) his seven migrate the way the repo's own was; (c) a fourth definition covering both shapes; (d) keep the prose definitions and let the library win silently. Whatever wins is bounded by decided constraints that are *not* part of the question: statuses subset the shared atoms (R-2), PRD-ish content is referenced rather than composed (R-9), and no plan-level task tier exists (R-11). Record the ruling **in the surviving file** and update or delete the losers — a ruling that lives only in a conversation does not exist.

**W-2 — the moment parser Cause 5 surfaces: what `PLAN-002-task-breakdown.md` is.** Plain frame: one of the seven files is a task list wearing a plan's filename — it has no scope, no objectives, no parts, and one relation; widening the plan rules to admit it would cost every real plan that protection. Assistant's recommendation: re-type or rename it out of the PLAN namespace. It is Peter's note — bring the proposal, do not move it unasked.

**W-3 — before the halt-site edits land: what a halted plan-part tells Peter to do, now that split is gone.** (Split's removal itself is decided — D-18; this is only the replacement text.) Plain frame: 18 lines across seven files currently tell a stuck session to recommend a mode that never executed; the replacement is what he will read at the exact moment he is already stuck, so a wrong one costs him at the worst time. Options: (a) halt for an explicit user scope decision, no automatic action — **assistant's recommendation**: no machinery dependency, and it is the honest shape of a halt; (b) where the halt is really "this work belongs in another plan", point at the plan-spawn flow — the flow itself is decided (R-10); whether halt text routes to it is this question; (c) invoke curation on the PLAN — only permissible **after** step 16's three adapter additions land; before then it swaps a non-executable mechanism for a silently lossy one; (d) raise the budget and continue — a fail-open branch; present it flagged as violating P4-12.

**W-4 — at the modes step, two consecutive questions.** *`--scope`*: (a) remove — **assistant's recommendation**: it is the read-only half of a removed mode, with no pipeline or reference file of its own; (b) keep as a report-only mode; (c) fold into W-3's halt answer. Its three live halt references get replace-before-remove regardless of the answer. *`migrate`*: (a) keep and fix the contradiction — **assistant's recommendation**; (b) remove. D-18 records Peter tempted to remove all three modes — that openness is why both halves are questions. If `migrate` stays, the fix direction is not open: it resolves toward the position the repo records as his verbatim clarification (*"no auto-migration in any form"*, `migrate-auto-detection.md:3`), and the false idempotency line is corrected under either answer.

**W-5 — at the part-id step, two consecutive questions.** *First, the rejected ids.* `stories`, `intl` and `build.deps` are the three real H3 parts the schema rejects in PLAN-002 (`:150`, `:221`, `:229`); `build.scaffold`/`build.tasks` are **table rows, not parts** — recorded as DONE in the summary table without any H3 ever authored — and ruling on rows changes nothing mechanical, so do not conflate the two. Peter authored the three by hand because non-SPEC build work — a dependency bump, a Storybook pass, an i18n sweep — had nowhere legal to live; that need is real and recurring, which is why this is genuinely open. Options: (a) delete them as parts and re-express as `build.SPEC-NNN` parts with TASK-level dependencies — **assistant's recommendation**; (b) widen `PartIdSchema` to admit a general non-SPEC build sub-part — say what else becomes expressible, and whether the widening also absorbs Cause 4's classes (bare phase names, `phase-N`, placeholders); (c) keep them as prose-only annotations. Bring one coherent part-id design, not four patches; where the answer covers Cause 4's classes say so, and where it does not, say that too. *Second, the `protocol-hardening` hardcode.* The shared regex hardcodes one project's phase names (`protocol-hardening`, `protocol-hardening\.[A-Z]+\.\d+`) into the library every consumer imports — delete, yes or no. Count live `protocol-hardening` parts in `docs/planning/PLAN-001-skills-ecosystem.md` first and state the count in the question text; that PLAN parses today and must not break as a side effect.

---

## Execution steps

1. **Load `skill-creator`; find the prompt-1 snapshot** (P4-5). Read everything in the read table and say what was read. Deliver the Step-0 maps; wait.
2. **W-1** at its moment. Record the ruling in the surviving file; update or delete the losing definitions.
3. **Fix the section model** `[PLAN-SECTION-MODEL]` — **committed before any parser change begins.** "Fixed" means a section present in the source survives a parse-render round trip; how (model every section, carry unmodelled H2s as opaque blocks in document order, or make the renderer a patcher) is a design default — pick, report. [DEFAULT] Non-negotiable: (1) `## Risks` (prefix-tolerant), `## Workflow Plan`, `## Decision Log`, `## Progress Log` and every phase H2 survive byte-for-byte unless a mutation deliberately changed them; (2) an unknown H2 is never silently dropped — fail closed or preserve (P4-12); (3) `## Progress Dashboard` and `## Cross-Part Dependency Graph` stay genuinely derived (that part of the current design is correct); (4) fix the two lying docstrings (`renderers/plan-note.ts:22-24`; `render-plan-note.ts:7-10`) in the same commit; (5) a round-trip test against a PLAN in the A+C shape — the honest gap: **no such test exists**; extend `plan-001-migration.test.ts`'s pattern rather than authoring a third harness (`plan-round-trip.test.ts` round-trips a fixture through `PlanAdapter`, not `parsePlanNote` — P4-13: read both first), and say which you did. Do not reorder on the grounds that the parser fix is "smaller" — that reasoning produces the destructive ordering.
4. **Define the status atoms** `[STATUS-ATOMS]` [DECIDED — R-2] — one shared status-atom vocabulary in `shared/composition/src/schemas/common.ts`, two families plus a small third: **artifact lifecycle** (DRAFT/PROPOSED/ACCEPTED/SUPERSEDED/…), **work progress** (PENDING/READY/IN_PROGRESS/BLOCKED/FAILED/DONE/DEFERRED/ABANDONED/…), **sessions** (IN_PROGRESS/PAUSED/DONE). Each note type declares a **subset** via `z.enum(Atom.extract([...]))` plus its own transitions. Same atom = same meaning everywhere; subset, never redefine. The listed atoms are decided; further additions are [DEFAULT] and reported. Part substatus becomes a work-progress subset (status quo seven values as the starting subset [DEFAULT]); the absence of SPLIT and DRAFT from part substatus stays correct — parts are work, not artifacts. Whether `PAUSED` is in the PLAN subset resolves the `plan-note-schema.md:27` vs `common.ts:51` contradiction — decide, report.
5. **Fix the parser** `[PLAN-PARSER-FIX]` — the six causes, with W-2 at Cause 5's moment; DoD and re-run obligations as stated in the census.
6. **Dead sections and rollups** `[PLAN-DEAD-SECTIONS]` `[PLAN-ROLLUP]` — [DECIDED — R-11]: tasks remain artifact-level in the SPEC subtree and surface in the plan **only as derived state; there is no plan-level task tier.** So the authored `## Tasks` register leaves the model — but not as a free deletion: read ADR-003 first (Chesterton's Fence — the three sections were consolidated to top level deliberately), give the 27 live rows a verified destination at the artifact level before anything is removed (P4-9) [DEFAULT on the destination], and amend `plan-001-migration.test.ts` AC#1b explicitly with Peter's approval, never silently. `## Pending User Decisions` and `## Editor Mirror IDs` land in step 14's audit with remove-or-re-home recommendations. `filter((_t) => false)` survives in no outcome. Delete the six H4 scaffolds in the same change that makes `:190` loud. Delete the hand-tick instruction and its anti-pattern row (`state-propagation.md:54`, `:108`).
7. **`d_n_substatus`** `[PLAN-DNSUBSTATUS]` — add the verbatim-decision field; reconcile the status enum toward the schema, declared as an R-2 subset; fix the prose. Prompt 7 depends on this.
8. **Make Contract 1 executable** `[PLAN-CONTRACT1-FIX]` — a call copied verbatim from any of the four doc sites executes and exits 0: an identifier→path resolver in the script [DEFAULT — an identifier is what a human types] or correct all four sites; report which. `--at-event` is read or removed — a required argument with no effect is worse than none. `--owning-session` is renamed (fix every call site) or documented.
9. **Halts and modes** `[SPLIT-REPLACE]` → `[SPLIT-REMOVE]`, `[SCOPE-MIGRATE]` — W-3, then replace all 18 lines, **then** remove the mode at every binding site, the frontmatter `description` first among them (replace-before-remove is forced: removing first leaves halts recommending a nonexistent mode at the worst possible moment). W-4 next; `--scope`'s three sites get replace-before-remove whatever the answer; the idempotency falsehood is corrected. `scope-evaluation-and-split.md` is deleted last, through the `rm` prompt.
10. **Part ids** `[Q25-SUBPARTS]` — W-5; apply the answers; address Cause 4's remaining classes in the same design or defer each with a reason.
11. **Session schema keys** [DECIDED — R-4, schema side only] — the composition session schema carries `started`, `ended`, `branch`, `plan` (permalink) and `parts` (list) alongside existing `status`/`type`/`tags`, with **no silent-discard of unknown-but-specified keys**. Rule: frontmatter for filtering, Relations for traversal, and a validator checks they agree. (Explicit date keys are what make date filtering real — `after_date` filters on index-modified time.) The session NOTE model — sections, cadence, invariants — is prompt 10's; touch nothing beyond these keys.
12. **Event-schema home** [DECIDED — R-6, home only] — the typed session-event enum schema lives in the composition library; land it there with R-6's candidate list as the starting set (SESSION_OPEN/PAUSE/RESUME/CLOSE, PART_TRANSITION, DISPATCH, ARTIFACT_WRITE, DECISION_LOCKED, GATE_RESULT, USER_RULING, COMMIT, HALT, CURATION_OP, CORRECTION). Prompt 10 owns note-side cadence and final membership.
13. **Inter-plan dependencies and BLOCKED parts** [DECIDED — R-10 for the flow; mechanics DEFAULT] — the plan-spawn handoff is a flow, not a file: detect out-of-scope → `/plan create` the dependency plan → link `depends_on`/`blocks` between the PLANs → mark the parent part **BLOCKED with a pointer** to the dependency → optional worktree → return. This prompt lands the data model only: inter-plan `depends_on`/`blocks` relations and BLOCKED-part-with-pointer semantics (schema, parse, render), so `/plan continue` can see why a part is blocked and what unblocks it. Routing is prompt 11's — build no router here.
14. **Redesign the template** `[PLAN-TEMPLATE]` — Peter's brief, verbatim: *the current plan template is not good at communicating status and sequence.* The PLAN's job is *"the status, the sequence, and the state of phases and their parts"*, and it is authored progressively, one phase at a time, each phase authoring its own section from the prior phase's artifacts [DECIDED — D-16]. Decided routings [DECIDED — R-9]: the PLAN **references** the PRD and never composes it — PRD-ish sections (overview beyond one line, product scope, product risks, success criteria) live in the PRD and are referenced like ANALYSIS/ADR/SPEC notes; risks split by kind — product risks → PRD, execution/sequencing risks (the pre-mortem rows behind `## Risks`, wave gates) stay on the plan; per-part exit criteria stay in parts. "Milestone" as plan-structure vocabulary is the planner agent's rival PLAN definition and dies where encountered here, as rival vocabulary rather than a rename [DECIDED — R-11]; the wider sweep is prompt 11's. Deliver the ranked **STAYS / MOVES / GOES** audit before proposing a template: every section across the union of the definitions (fullest specimen: `PLAN-001-fond`, 20 H2s), one bucket each, ranked within bucket, one-line recommendation and rationale per row — approved **before** any section is removed. Then a proposed template and one of Peter's real PLANs re-rendered against it, so he sees output rather than description. Delete nothing from his notes without approval (P4-9).
15. **The nine fictions** `[FIC-ACT]` — each is `implement`, `delete`, or `explicitly accept`; there is no fourth option and "document and move on" is not one (P4-12):

| # | Claim | Reality / where resolved |
|---|---|---|
| FIC-2 | `plan-note-schema.md:3` "Authoritative schema", incl. the six H4s and `d_n_substatus.decision` | H4s emitted/read by nothing; verbatim field absent (steps 6, 7) |
| FIC-4 | `validateIntegrityFloor` is live (`schemas/base.ts:105`) | three invocations, all in one test file (step 16) |
| FIC-10 | the adapter safely splits a PLAN | it cannot (step 16) |
| FIC-11 | Tasks / PUD / Editor Mirror IDs are part of the model | 0 of 7 of Peter's; populated and test-asserted in the repo's own — resolve as "explicitly accept, scoped" unless the ADR-003 read says otherwise (step 6) |
| FIC-14 | Contract 1 is the legal phase-completion signal | every documented invocation exits 2 (step 8) |
| FIC-15 | mechanical enforcement protects PLAN operations | never executed against Peter's seven; does pass against the repo's own via the migration test — state the scope when resolving |
| FIC-16 | the renderer "regenerates the two derived sections" | it regenerates the entire document (step 3) |
| FIC-32 | adapter seam metadata (`sectionDelimiter`/`identifierPattern`/`identifierPrefix`, abstract at `core/base-markdown-adapter.ts:14-16`) is honored | no method body in any adapter reads any of them (grep-confirmed). `PlanAdapter` is worse: `plan.ts:52` implements `CompositionAdapter` without extending `BaseMarkdownAdapter`, declaring unrelated snake_case twins (`section_delimiter` `:59`, `identifier_pattern` `:67`, no `identifierPrefix` equivalent) — the case split is the tell the seam contract was never adopted; cite it that way |
| FIC-33 | `PlanAdapter.extractBySectionName` (`plan.ts:127-144`) does heading-aware extraction | reachable only via the `:106-107` dispatch (it is `private`, `:127`) and dead in practice because `collectClusterRanges` never emits a `section` range — fix why the cluster layer only produces numeric ranges, not just the wording |

Also record under step 15, do not touch: `lock-decision-mutation.ts:110-128` flips the decisions part to DONE six gates and the entire ADR early — **prompt 7 owns the fix**.
16. **Harden the adapter** `[ADAPTER-HARDEN]` — three additions (need decided by the facts above; design [DEFAULT]): (1) parse the split result through `PlanNoteSchema` and run `validatePlanDoneClaim` post-split — closes the part-status and `depends_on` gaps; (2) re-render, not merely exclude, `regenerated_sections` after a PLAN split — **depends on step 3**; before the section model is fixed this builds the destruction into the split path; (3) wire `validateIntegrityFloor` into the CLI (P4-13: wire, don't rebuild). Conditional edge: these are a hard prerequisite of the halt replacement **iff** W-3 chose curation-on-a-PLAN; otherwise the ordering is free.
17. **Re-author the four plan-state memories into `plan`** `[AM-MIG-SKILLS]` — destination-first re-authoring per R-26: the memory is an input, never the artifact; the layer is **thinned, not eliminated** [DECIDED — R-1], and the source files stay in place, untouched, for prompt 12's owner review. No `feedback_*` filename is cited in any destination text; each rule lands inline and self-contained (prompt 1 made the thirteen TypeScript citations self-contained for exactly this reason — do not undo it).

| Memory (source, stays in layer) | Substance to re-author | Destination |
|---|---|---|
| `feedback_phase_not_done_while_plan_has_pending_items` | the PLAN is the dynamic state indicator; its pending items are the authoritative signal of phase completeness. **Carries EH-10 — see below** | `plan/SKILL.md` body, near the part-status rules |
| `feedback_session_task_completion_gated_on_plan` | the verification procedure behind "no completion flip until PLAN Exit Criteria are all `[x]`" | `plan/references/` |
| `feedback_phase_skill_wins_conflicts` | D-13/P4-11 precedence, with the concrete trap named (authoring a PLAN from the STRUCTURES-4.6 Phase/Milestone shape instead of the skill's schema — also R-11's rival-vocabulary case) | `plan/SKILL.md` body, standing statement |
| `feedback_workflow_phase_rigor_at_every_layer` | **rewrite before landing**: it mandates the auto-memory layer as an enforcement layer (`:27`, `:50`), and its dispatch-brief claim was established false by prompt 2 | rewrite, then `plan/references/` |

**EH-10** [DECIDED — forced by P4-12]: the hatch inside the first memory — *"or explicitly close it with documented rationale"* — is a textbook rationale-and-proceed branch sitting inside the very rule whose thesis is that the PLAN is authoritative. It does not migrate in any form. The replacement: a pending item is closed by **resolving it** or by **an explicit terminal-substatus transition on the item itself** (`DEFERRED`/`ABANDONED`, per R-2's work-progress family, visible in the PLAN's own state). Record `EH-10 — deleted, replaced by terminal-substatus transition` beside the fictions. The wave-6b memories (`feedback_proactive_parallelism_check`, `feedback_no_unfounded_subagent_model_override`, `feedback_iterative_phase_reentry_on_gap_discovery`) are prompts 8/9's — leave them alone.
18. **Independent evaluation** (P4-4/D-2) — `plan` and every skill touched, cold-read, beyond this brief; the seeds above may be folded in or rejected; ranked findings; nothing applied without approval.

Any script, test harness or tooling this prompt authors is **pure Bun TS** — never Python, never Node [DECIDED — R-21].

## Boundary

- The session NOTE model — sections, event cadence, enum membership, session invariants — is prompt 10's (R-3/R-5/R-6/R-7). This prompt lands only the R-4 schema keys and the R-6 schema home.
- R-10 routing (who detects out-of-scope, who spawns, the curation job) is prompt 11's; only the data model lands here.
- `documentation/workflows.html` and the home specs: record the `PhaseEnum` mismatch, change nothing — prompt 11 owns that surface.
- `lock-decision-mutation.ts`'s early DONE flip: recorded, untouched — prompt 7.
- Wave-6b memories and the `references/dispatch.md` / `references/agent-teams.md` destinations: prompts 8/9.
- `decompose`/`recompose`/`defrag` skill bodies are prompt 4's; this prompt touches the composition library and the `plan`/`research`/`decisions`/`build`-reference surfaces named above, nothing else.
- Atom subsets for ADR/SPEC/SESSION note types beyond the homes above: prompts 7, 8 and 10 declare their own subsets against the vocabulary defined here (R-2).

## DECIDED — do not re-litigate

| Decision | Source |
|---|---|
| One shared status-atom vocabulary in `common.ts`; artifact-lifecycle + work-progress families, sessions a small third; every note type subsets via `z.enum(Atom.extract([...]))` with its own transitions; same atom = same meaning; subset, never redefine | R-2 |
| The PLAN references the PRD, never composes it; PRD-ish sections live in the PRD; product risks → PRD, execution/sequencing risks stay on the plan; per-part exit criteria stay in parts | R-9 |
| Parts stay; phases stay; "milestone" dies as rival vocabulary, not as a rename; tasks stay artifact-level in the SPEC subtree — no plan-level task tier, derived state only | R-11 |
| The plan-spawn handoff flow (out-of-scope → `/plan create` → `depends_on`/`blocks` → parent part BLOCKED with pointer → optional worktree → return); this prompt: relations + BLOCKED semantics; prompt 11: routing | R-10 |
| The composition session schema carries `started`/`ended`/`branch`/`plan`/`parts` with no silent-discard; frontmatter filters, Relations traverse, a validator checks agreement | R-4 |
| The typed session-event enum schema homes in the composition library | R-6 |
| The PLAN's job is the status, the sequence, and the state of phases and their parts; authored progressively, one phase at a time | D-16 |
| `--split` is removed; curation is that job | D-18 |
| The PLAN is not the dispatch-brief carrier; `build_workflow_items` stays a derived rollup | R-15/R-16 (executed in prompt 2); R-11 |
| Section model before parser; the section-model change is committed before parser work begins; `render-plan-note.ts` runs against no real PLAN until then | forced by the destruction sequence |
| All halt text replaced before the mode is removed | forced by the dangling-pointer failure |
| EH-10 deleted: a phase is never closed over pending PLAN items on a written rationale | forced by P4-12 |
| Any script or tooling authored here is pure Bun TS | R-21 |
| The memory layer is thinned, never eliminated; migration is destination-first re-authoring | R-1, R-26 |
| Update cost is work, not an argument | D-19 |

## Git

Verify the branch first; one branch; coherent commits — the section-model commit lands **before** any parser change. Unmerged, no push, no `--no-verify`, no AI attribution. Every deletion through the `Bash(rm:*)` gate; no workarounds; the report says so.

## Done means

- [ ] Step 0's maps delivered and approved **before the first edit**.
- [ ] **Every W item was raised at its marked moment, once, in plain language, and its answer is recorded verbatim in the report. No W item was decided silently, and none was asked twice.**
- [ ] One PLAN definition exists; the ruling is recorded in the surviving file; the losing definitions are updated or deleted; the disposition of `plan-001-migration.test.ts` and REQ-012-SPEC-007 is recorded either way.
- [ ] The section model preserves every H2, committed **before** any parser change — the two commit SHAs shown in order; both lying docstrings fixed in that commit.
- [ ] The status atoms live in `common.ts` per R-2; every status enum this prompt touched is a declared subset; additions beyond the listed atoms are reported as defaults; the PAUSED contradiction is resolved and reported.
- [ ] `parsePlanNote` succeeds on all six real PLANs that are PLANs (plus the recorded W-2 ruling) — demonstrated by running it and pasting the output — and they round-trip with empty diffs: `## Risks` (incl. `## Risks (pre-mortem)`), `## Workflow Plan`, `## Decision Log`, `## Progress Log`, every part-bearing H2.
- [ ] An A+C-shape round-trip test exists, extending `plan-001-migration.test.ts`'s pattern; the two existing round-trip tests were read before it was written.
- [ ] The full composition suite passes — report the number it reports — and `plan-001-migration.test.ts` is green or its amendment was approved by Peter.
- [ ] R-11 applied: no plan-level task tier; the 27 live `T-NN` rows have a verified artifact-level destination before anything is removed; `## Pending User Decisions` / `## Editor Mirror IDs` dispositioned through the audit after ADR-003 was read; `filter((_t) => false)` is gone.
- [ ] The six H4 scaffolds are deleted and `parseBuildWorkflowItems` fails loudly on malformed items.
- [ ] `## Progress Dashboard` is derived-only; the hand-tick instruction and its anti-pattern row are gone.
- [ ] `DecisionStateSchema` holds the verbatim decision text; its status enum is reconciled as an R-2 subset; the prose is fixed.
- [ ] A Contract 1 call copied verbatim from any of the four doc sites executes and exits 0; `--at-event` is read or removed; `--owning-session` is renamed or documented.
- [ ] All 18 split-naming lines carry their replacement **before** the mode is removed; every binding site is clean, `plan/SKILL.md:3`'s frontmatter `description` included; the gate `grep -rniE -- '(--split|/plan split|args="split)' skills/` returns 0 outside history — a single-spelling grep is not the gate.
- [ ] `--scope` and `migrate` rulings applied at every site — `--scope`'s three halt references replaced before any removal; the false idempotency claim at `workflow-migration.md:246` corrected.
- [ ] The W-5 rulings are applied; Cause 4's remaining id classes are addressed or each deferred with a reason; the `protocol-hardening` answer is acted on with the repo's own PLAN still parsing.
- [ ] The adapter's three additions are in place and `validateIntegrityFloor` has at least one production call site.
- [ ] The session schema carries the five R-4 keys with no silent-discard, plus the frontmatter/Relations agreement validator; the event enum schema homes in the library; nothing of prompt 10's note model was touched.
- [ ] Inter-plan `depends_on`/`blocks` and BLOCKED-with-pointer parse and render; no router was built.
- [ ] The ranked STAYS/MOVES/GOES audit — every section, one bucket each, ranked, recommendation + rationale per row — was delivered and approved **before** any section removal; R-9's routings are honored; one real PLAN re-rendered against the proposed template and shown.
- [ ] Each of the nine fictions is fixed or explicitly accepted with the disposition recorded — FIC-11/FIC-15 scoped as narrowed, FIC-33 fixed at the cluster layer or explicitly accepted; `EH-10 — deleted, replaced by terminal-substatus transition` is recorded beside them and the hatch phrase appears nowhere in `plan/SKILL.md`.
- [ ] The four memories are re-authored destination-first, inline and self-contained, no `feedback_*` filename cited; `feedback_workflow_phase_rigor_at_every_layer` was rewritten before landing; the source files remain in the layer untouched.
- [ ] `skill-creator`'s lifecycle ran for every skill touched under its own gates; its validator passes; evals pointed at the prompt-1 snapshot with no benchmark regression; any skipped stage is named with its reason.
- [ ] Every deletion went through the `rm` gate with no workaround; any declined deletion is reported, not worked around.
- [ ] Ranked independent-evaluation findings delivered; nothing applied without approval.
- [ ] Branch verified; commits coherent; nothing pushed; no AI attribution in any message.
