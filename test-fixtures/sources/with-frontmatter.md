---
title: With Frontmatter
type: analysis
status: DRAFT
---

# With Frontmatter

This source file already has YAML frontmatter that declares a canonical entity type.
It should be detected via the frontmatter `type` field and routed to the analysis folder.
